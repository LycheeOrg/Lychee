# Feature Plan 063 – Album Timeline Buckets Frontend Adoption

_Linked specification:_ `docs/specs/4-architecture/features/063-album-timeline-buckets-adoption/spec.md`
_Status:_ In Progress — I1-I9/I11/I13/I14/I15 implemented (I14: 2026-09-02 smart-album-tiles slice; I15: 2026-09-03 full root-gallery SoA scope, Q-063-16 — own/shared root bucketed grid, tags/persons/pinned flat swaps, root drag-select support — code-complete, `make phpstan`/`php-cs-fixer`/`npm run check`/`npm run format` clean, `AlbumCategoryV3Test`/`AlbumRootV3Test`/`PhotoAssetV3Test`/full `Album*V3Test` sweep green (150+ tests), manual browser verification of S-063-27..40 not performed this session, no dev server/database available); I10 (manual Tag/Person verification, no dev environment available this session), I12 (api-design.md/knowledge-map.md/roadmap.md updates), and the Implementation Drift Gate still pending.
_Last updated:_ 2026-09-03

> Guardrail: Keep this plan traceable back to the governing spec. Reference FR/NFR/Scenario IDs from `spec.md` where relevant, log any new high- or medium-impact questions in [docs/specs/4-architecture/open-questions.md](../../open-questions.md), and assume clarifications are resolved only when the spec's normative sections (requirements/NFR/behaviour/telemetry) and, where applicable, ADRs under `docs/specs/5-decisions/` have been updated.

## Vision & Success Criteria

Let a viewer scroll a 7,000+-direct-child album's subalbum grid as smoothly as a 20-child one — sticky date/title-prefix headers rendered from the server's own bucket aggregation, right-click actions correct the instant they're needed, cover pixels fetched lazily per mounted tile, and drag-select working across the whole list, not just whatever happens to be on screen — all while the flag-off path stays byte-for-byte what it is today. Success is measured by: DOM node count for the new tile component staying bounded (viewport + overscan) regardless of total child count, verified against a 7,000+-child fixture (NFR-063-03); client-side rights combination matching direct `AlbumPolicy` calls exactly across guest/regular/owner/admin/multi-group-overlap callers (NFR-063-04); and every one of Feature 056's, Feature 058's, and Feature 061's already-shipped pieces (`Thumb.vue`/`ThumbAssetService`/the Asset endpoint, `AlbumListState`, the three v3 endpoints) reused as-is with zero modification.

## Scope Alignment

- **In scope:** `AlbumState.ts` (singular per-navigation store) extended with a tier-1+2 fetch pipeline (`loadAlbumsV3()`, dispatched via `loadAlbumsAuto()`) plus a positional-walk boundary derivation (no join or sort needed — tier 2 already arrives ordered per Feature 061 FR-061-26) and a tier-3 background-fetch/rights-merge pipeline (`loadAlbumsV3Rights()`), flag-gated, guarded against stale cross-album responses by the store's existing `requestedAlbumId` pattern (FR-063-18) rather than a new generation counter; response caching (FR-063-19) reuses this codebase's existing `axios-cache-interceptor` convention (a new `AlbumChildrenV3Service`) rather than a bespoke in-store cache; adapted tiles are written into the existing `AlbumsState.ts` (plural)'s `albums` field, unchanged shape. A new pure row-flattening composable (`virtualAlbumRows.ts`) shared by grid and list rendering, computing grid tile-row heights analytically from the parent album's known `album_thumb_css_aspect_ratio` rather than measuring them; a new analytic `itemsPerRow`/tile-width composable (`albumTileWidth.ts`, FR-063-14) measuring the grid's own root element (`useElementSize()`) and `useBreakpoints(breakpointsTailwind)`'s default thresholds (`3xl`/`4xl` confirmed dead in the actual compiled CSS, not modeled), no probe tile, no reuse of the unrelated photo-layout `getWidth.ts`; a new virtualized grid component (`AlbumThumbGridVirtual.vue`), virtualized list-view component (`AlbumListViewVirtual.vue`), a small `AlbumThumbPanelVirtual.vue` grid/list dispatcher, and two per-child tile components (`AlbumThumbVirtual.vue`/`AlbumListItemVirtual.vue`, forked from `AlbumThumb.vue`/`AlbumListItem.vue` respectively, each resolving its cover image via the existing Feature 056 Asset endpoint instead of a pre-built URL — tier 2 supplies only `cover_id`, no URL), all under a new `.../albumModule/Virtualized/` directory (kept separate from the existing flag-off components so they can be removed as a unit later) and built on `@tanstack/vue-virtual`'s `useWindowVirtualizer` (not `AlbumNavTree.vue`'s own-scroll-box `useVirtualizer` — `AlbumPanel.vue`'s content shares one ordinary page scroll) following its `translate3d`/single-spacer conventions otherwise; a new `phpDateFormat.ts` utility reproducing PHP `date()` semantics client-side, feeding the adapter's `formatted_min_max` computation (FR-063-16); client-side rights-combination logic, gated off for `TagAlbum`/`PersonAlbum` parents (`isRegularAlbumParent`) rather than merely "reducing" for them; adapting joined v3 data (now including Feature 061's new `is_pinned`/`is_public`/`is_link_required` fields, FR-061-27) into `ThumbAlbumResource`-shaped metadata/rights objects so `contextMenu.ts` needs no changes; `TagAlbum`/`PersonAlbum` parity via 061's existing tier 2/3 support; reimplementing `dragAndSelect.ts`'s album-selection branch against an independently-recomputed instance of the same precomputed tile geometry instead of DOM queries, scoped to the flag-on virtualized path; hiding the album `<Pagination>` control for the flag-on path — re-fetch-on-mutation (now including cover-photo changes, FR-063-13) needed no new wiring, since it was found to already be satisfied by the pre-existing `emits("refresh")` → `albumStore.refresh()` cascade.
- **Out of scope:** Photo-grid virtualization (any layout mode); root-scope (`Albums.vue`) bucketing/virtualization; any further v2/v3 backend change beyond FR-061-27; any change to `Thumb.vue`/`ThumbAssetService`/the Feature 056 Asset endpoint itself (reused as-is); any change to `AlbumThumb.vue`/`AlbumThumbImage.vue`/`AlbumListItem.vue` themselves or the flag-off cover-image/date-subtitle path; any change to `dragAndSelect.ts`'s photo-selection branch or its flag-off album branch; a new feature flag; an automated frontend test suite; a windowed/paginated variant of tier 2; any client-side sorting or grouping of tier 2's rows (the backend already guarantees their order); a general-purpose PHP-date-format-to-JS library extracted for reuse elsewhere (`phpDateFormat.ts` is scoped to this feature's own needs).

**2026-09-02 addendum — I14, smart-album root tiles (Q-063-14):**
- **In scope:** `AlbumsState.ts`'s root `load()` action gains a flag-gated `loadSmartAlbumsV3()` sourcing `baseSmartAlbums` from Feature 062's `GET /Albums/smart` instead of `Top::get()`'s `smart_albums` field (FR-063-20), via a new `AlbumCategoryV3Service` (DO-063-10) and a new `adaptSmartAlbumTile.ts` adapter (DO-063-11, FR-063-21) reusing the existing `AdaptedAlbumTile` type (DO-063-01). `AlbumThumbImage.vue` gains a small additive `coverId`/`albumId`-prop branch resolving via `<Thumb>`/the Feature 056 Asset endpoint when `thumb` is absent (FR-063-22) — unlike the subalbum-grid consumer, **no fork** here: the delta from today's behavior is one optional branch on one already-shared leaf component, not a structurally different rendering strategy, so this repo's "fork a shared component rather than branch a flag into it" convention doesn't apply the same way (that convention is about *virtualization vs. not*, a real structural fork; this is *cover-image source A vs. B* on an otherwise-identical render path). Two small backend amendments, each owned by and living in its source feature's own files: `AlbumCategoryController::smart()` (Feature 062) gains a batched `AlbumUserThumb::whereIn('album_id', $ids)->where('user_id', $user_id)` lookup — one indexed query, not per-row, not a live `photos` query (FR-062-16); `GetPhotoAssetRequest::isPhotoOfAlbum()` (Feature 056) gains a `BaseSmartAlbum` branch mirroring its existing `TagAlbum`/`PersonAlbum` cache-exception check (FR-056-08).
- **Out of scope:** The other four category/root endpoints Feature 062 shipped (`/Albums/persons`, `/Albums/tags`, `/Albums/tags/rights`, `/Albums/pinned`, `/Albums/root[/buckets|/rights]`) — all stay v2-sourced (NFR-063-09); any virtualization/windowing of the Smart Albums section (bounded, small set — no benefit); any change to `AlbumThumbVirtual.vue`/`AlbumThumbGridVirtual.vue`/the other subalbum-grid virtualization components (unrelated consumer); any change to `Top::get()`/`Actions\Albums\Top` itself (still computes `smart_albums` for the flag-off path, untouched). **Superseded by I15 below (Q-063-16) — every item in this "Out of scope" bullet except the last is now in scope.**

**2026-09-03 addendum — I15, full root-gallery SoA scope (Q-063-16/17), supersedes I14's "Out of scope" bullet above:**
- **In scope:** The four remaining Feature 062 endpoints, adopted the same session: `GET /Albums/tags`+`/tags/rights` (real rights, `combineTagAlbumRights()`), `/Albums/persons?scope=`, `/Albums/pinned?scope=` (both own+shared fetched and merged client-side, no rights endpoint per NG4 — safe-default rights, an accepted gap), and `/Albums/root[/buckets|/rights]?scope=own|shared` — own/shared root albums get the *same* virtualized/bucketed grid sub-album children already have, via new root-scope forks of the Virtualized/ components (`AlbumRootGridVirtual.vue`/`AlbumRootListViewVirtual.vue`/`AlbumRootPanelVirtual.vue`, Q-063-17) rather than a prop-driven generalization of the originals — the pure functions (`buildVirtualAlbumRows`, `computeBucketBoundaries`, `useAlbumTileWidth`) and the leaf tile component (`AlbumThumbVirtual.vue`) were already store-decoupled and needed zero changes; only the three store-coupled orchestration components are forked. `AlbumsState.ts` gains parallel own/shared bucket-tier state (`ownBucketsV3`/`ownBoundariesV3`/`sharedBucketsV3`/`sharedBoundariesV3`, plus a new flat `sharedAlbumsV3` field distinct from the flag-off `sharedAlbums`'s grouped-`SplitData` shape) and five new actions, all fetched in parallel from `load()`'s existing v2/v3 branch. `combineAlbumChildRights()` refactored to take a precomputed `isOwner: boolean` (root's own/shared partitioning happens at the SQL level, not via a per-response `owner_id` comparison the way sub-album children's does) — the sub-album call site's original derivation extracted as `isRegularAlbumParentOwner()`, behavior unchanged there. `dragAndSelect.ts`'s `getAlbumBoxesV3()` generalized to query every `[data-album-grid-root]` (not just the first) and branch per a new `data-album-grid-scope` attribute, since root's non-tabbed SHOW mode can mount own+shared grids simultaneously — a case the original sub-album-only version never had to handle. `AlbumRootPanelVirtual.vue` gains a simple (non-collapsible) section title for `own` scope, closing a gap found immediately after mounting the first version (v2 always shows a "My Albums" title; the first draft of the virtualized grid didn't). Two unrelated pre-existing bugs surfaced and fixed along the way: `GetPhotoAssetRequest::rules()` used `RandomIDRule` instead of `AlbumIDRule` for `album_id` (blocking every smart-album Asset request at validation, before FR-056-08's new branch could ever run); `RootConfig.php`'s `album_thumb_css_aspect_ratio` carried a stray `#[LiteralTypeScriptType]` override forcing the wrong generated TS type, latent until this addendum's root grid became the first consumer to read that field for numeric aspect-ratio math.
- **Out of scope (still):** Photo-grid virtualization; any collapse/expand toggle for the new root sections' titles (FR-063-29's deliberate simplification); resolving `is_public`/`is_link_required`/etc. for the four flat category listings beyond what `AlbumChildrenDataResource` already gives root's own/shared tiles (an honestly-documented gap for smart/tags/persons/pinned specifically, tracked in `adaptCategoryTile.ts`'s own doc comment); the `SEPARATE_SHARED_ONLY` shared-albums-visibility mode's client-side public-album filter, when applied on top of server-computed shared-root buckets — filtering tiles client-side after tier1's bucket counts were computed server-side would desynchronize the two, tripping `computeBucketBoundaries()`'s mismatch fallback; not addressed this pass, a known, narrow, low-likelihood-of-notice gap (this mode is one of several `shared_albums_visibility_mode` options, not the default).

## Dependencies & Interfaces

- Feature 061's three endpoints and resources: `AlbumBucketResource`/`AlbumBucketController`, `AlbumChildrenDataResource`/`AlbumChildrenDataController` (orders its rows per FR-061-26), `AlbumChildrenRightsResource`/`AlbumChildrenRightsController` (consumed as-is, zero further backend changes).
- `App\Policies\AlbumPolicy::canEdit()`/`canDownload()`/`canDelete()` (`app/Policies/AlbumPolicy.php:255-269,184-207,281-303`) — reference-only, the ground truth the client-side combination formula (FR-063-04) must match; not called or modified by this feature.
- `resources/js/stores/AlbumsState.ts` (plural, extend) — existing store backing `AlbumPanel.vue`'s subalbum section (`albums` field); adapted tiles are written here, unchanged shape.
- `resources/js/stores/AlbumState.ts` (singular, extend) — the tier 1/2/3 fetch pipeline itself (`bucketsV3`/`boundariesV3`/`bucketableV3`/`loadAlbumsV3()`/`loadAlbumsV3Rights()`/`loadAlbumsAuto()`) lives here, alongside `albumId`/navigation state and the v2 `loadAlbums()`/`loadMoreAlbums()`, which remain the flag-off path, untouched.
- `resources/js/stores/UserState.ts` (`useUserStore()`) — `user.id`/`isGuest`/`isAdmin`, needed for FR-063-04's combination formula. `may_upload` is not exposed here — `albumsStore.rootRights?.can_upload` is used instead (`UserResource` never surfaces the underlying column to the frontend).
- `resources/js/v8/components/thumbs/Thumb.vue` (Feature 056's Asset endpoint, `ThumbAssetService`-backed, reused unchanged) — cover-image resolution for each mounted tile, consumed by the new `AlbumThumbVirtual.vue` (DO-063-06) the same way `AlbumNavTree.vue` already consumes it for its own tree nodes.
- `resources/js/services/thumb-asset-service.ts`'s `ThumbAssetService.acquire()`/`release()` (reused unchanged, internal to `Thumb.vue` — not called directly by this feature) — LRU-cached, ref-counted blob-URL fetch against `GET /api/v3/Asset/{album_id}/{photo_id}/{size_variant}` (Feature 056, API-056-01).
- `resources/js/v8/components/gallery/albumModule/thumbs/AlbumThumb.vue`/`AlbumThumbImage.vue`/`AlbumThumbOverlay.vue`/`AlbumThumbDecorations.vue`/`ThumbBadge.vue` — fork source for `AlbumThumbVirtual.vue` (DO-063-06): the overlay/decoration/badge sub-components are reused unchanged, `AlbumThumbImage.vue`'s pre-built-URL logic is the one piece not reusable (tier 2 has no URL to feed it) and is replaced with `<Thumb>`.
- `resources/js/v8/components/gallery/albumModule/AlbumNavTree.vue` — existing `@tanstack/vue-virtual` usage in this codebase; a general convention reference for DO-063-03/04 (`translate3d` positioning, single spacer sized to `getTotalSize()`, `contain` hints), not a template whose internals must be replicated exactly — besides the multi-column/sticky-header/aspect-ratio-height requirements, it also scrolls inside its own fixed-height sidebar box (`useVirtualizer`), whereas DO-063-03/04 share `AlbumPanel.vue`'s ordinary page/window scroll (`useWindowVirtualizer` with a `scrollMargin` tracking the grid's own document-relative top offset). Also the existing precedent for `<Thumb>` usage that DO-063-06 mirrors.
- `App\Http\Resources\GalleryConfigs\AlbumConfig`/`RootConfig`'s `album_thumb_css_aspect_ratio` field — already loaded for the currently-browsed album via `AlbumState.ts` before this feature's tier 1/2/3 fetches begin; the input to FR-063-14's row-height math, resolved with the same per-album-override-then-instance-default fallback chain `AlbumThumb.vue` already uses. Extended (FR-063-16) with two new fields, `date_format_album_thumb`/`thumb_min_max_order`, same resolution chain.
- `resources/js/layouts/getWidth.ts` — existing container-width-measurement pattern used today by the photo-layout composables (`useGrid.ts`/`useSquare.ts`/`useJustify.ts`/`useMasonry.ts`); a different, unrelated, imperative measurement (`window.innerWidth` minus a hardcoded scrollbar-width guess) — **not** reused by FR-063-14's `albumTileWidth.ts` (DO-063-09), which instead measures the grid's own root element reactively via `useElementSize()`.
- `useBreakpoints`/`breakpointsTailwind` (`@vueuse/core`) — existing usage in this codebase (`AlbumNavPanel.vue`, `PhotoShareCard.vue`, `useAdminTiles.ts`); reused by FR-063-14 with its **default** `sm`/`md`/`lg`/`xl`/`2xl` thresholds — confirmed during implementation, via the actual compiled production CSS bundle, that this project defines no `--breakpoint-3xl`/`--breakpoint-4xl` custom property, so `3xl:`/`4xl:`-prefixed classes (including `AlbumThumb.vue`'s own) are silently dropped by Tailwind v4 at build time and stay unmodeled — `2xl`'s formula is the one that already applies at `2xl` and beyond.
- `resources/js/v8/components/gallery/albumModule/AlbumListItem.vue`/`ListBadge.vue` — fork source for `AlbumListItemVirtual.vue` (DO-063-07), same relationship `AlbumThumb.vue` has to `AlbumThumbVirtual.vue` (DO-063-06): badge/layout logic reused unchanged, its own `AlbumThumbImage.vue` usage is the one piece replaced with `<Thumb>`.
- `ThumbAlbumResource::formatMinMaxDate()` (`app/Http/Resources/Models/ThumbAlbumResource.php:116-132`) — reference-only, the exact logic FR-063-16's `phpDateFormat()`/date-join must reproduce client-side; not called or modified by this feature. A community PHP-date-to-JS format-string conversion function (public gist, adapted) is the starting point for `phpDateFormat.ts` (DO-063-08) rather than writing a PHP `date()` interpreter from scratch.
- `resources/js/services/album-service.ts`'s `AlbumService`/`axios-cache-interceptor` convention — reused directly (not mirrored) by FR-063-19's new `AlbumChildrenV3Service`; `AlbumService.clearCache()` was extended to also clear the three new v3 cache entries (Q-063-12 Option B).
- `@tanstack/vue-virtual` (already a direct `package.json` dependency — no new install).
- **(2026-09-02 addendum)** `App\Http\Controllers\Gallery\AlbumListing\AlbumCategoryController::smart()` (Feature 062) — amended, not called directly by this feature's own code, but its response shape (`AlbumCategoryListResource`) is this addendum's sole new fetch target (FR-063-20).
- **(2026-09-02 addendum)** `App\Http\Requests\Photo\GetPhotoAssetRequest::isPhotoOfAlbum()`/`rules()` (Feature 056, FR-056-08) — amended (T-056-19, attributed to Feature 056's own task list, though implemented as part of this addendum's I14) so a smart album's cached cover survives a live-condition staleness window, and so `album_id` validation (`AlbumIDRule`, not `RandomIDRule`) actually accepts a `SmartAlbumType` value at all — through the same Asset endpoint `AlbumThumbImage.vue`'s new branch (FR-063-22) calls.
- **(2026-09-02 addendum)** `resources/js/stores/AlbumsState.ts`'s `load()`/`reset()` (`AlbumsState.ts:67-117,51-61`) — `load()` gains the `is_struct_of_array_enabled` branch dispatching to `loadSmartAlbumsV3()`; `reset()` needs no change (`baseSmartAlbums = []` already correct for either source).
- **(2026-09-02 addendum)** `resources/js/v8/components/gallery/albumModule/thumbs/AlbumThumbImage.vue`/`AlbumThumb.vue`/`AlbumListItem.vue` (list view, found during implementation to need the same wiring) — extended (not forked) with the new `albumId`/`coverId` prop pair and `<Thumb>` fallback branch (FR-063-22); every existing v2 caller (subalbum flag-off tiles, tag/person root tiles) passes neither prop and is unaffected.
- `resources/js/v8/components/gallery/albumModule/AlbumThumbPanel.vue`/`AlbumThumbPanelList.vue`/`AlbumListView.vue` — branch point: render the new virtualized components instead of today's plain `v-for` when the flag is on and v3 data is present; otherwise fully unchanged.
- `resources/js/v8/composables/contextMenus/contextMenu.ts` — read-only dependency; the exact field reads (`selectedAlbum.rights.can_edit`/`can_move`/`can_delete`/`can_download`) this feature's adapted `ThumbAlbumResource` objects must satisfy (FR-063-06).
- `resources/js/composables/album/dragAndSelect.ts` (extend the album-selection branch only) — `dragAndSelect.ts:187-190`'s current `document.querySelectorAll` call site.
- `resources/js/v8/components/gallery/albumModule/SelectDrag.vue`, `AlbumPanel.vue` (`#galleryView` scroll container) — existing mount points, unchanged.
- `resources/js/services/album-service.ts`'s `AlbumService.clearCache()` convention — the existing invalidation-trigger call sites (`AlbumDelete.vue`, `DeleteDialog.vue`, `Unlock.vue`, `AlbumVisibility.vue`) this feature's own re-fetch (FR-063-13) is wired alongside.

## Assumptions & Risks

- **Assumptions:** The 7,000+-direct-children scale figure Feature 061 confirmed remains the operative target; `@tanstack/vue-virtual`'s documented sticky-header pattern (flattened row list + a custom range extractor keeping the active section's header row always rendered) is sufficient without needing a second, independent sticky-positioning mechanism; Feature 061's FR-061-26 ordering guarantee (tier 2's row order matches tier 1's bucket boundaries exactly) holds for every response this feature reads.
- **Risks / Mitigations:**
  - *Risk:* Reimplementing `dragAndSelect.ts`'s album-selection branch (FR-063-11) against precomputed geometry could silently diverge from the DOM-query branch's exact selection semantics (e.g. edge/corner intersection rules). *Mitigation:* write the geometry-intersection test first against the same fixture/rectangle cases the existing DOM-query branch already handles correctly today (flag-off), confirming byte-for-byte identical selections before extending to the unmounted-tile case DOM-query cannot handle at all.
  - *Risk:* Tier 1 and tier 2 are two separate requests (FR-063-01) — a mutation racing between them (e.g. a subalbum deleted by another session mid-navigation) could produce a `sum(counts) !== children.length` mismatch even though each response is individually correct and correctly ordered. *Mitigation:* FR-063-02's defensive single-unbucketed-section fallback for a count mismatch, plus FR-063-13's re-fetch-on-mutation covers the same-session case; a genuinely concurrent cross-session edit is an accepted, rare staleness window (matches Feature 061's own accepted-staleness precedent for its rights-endpoint cache, FR-061-22).
  - *Risk:* `itemsPerRow` (FR-063-07/14) depends on the scroll container's content width and the current Tailwind breakpoint, both of which can change (window resize, nav-panel toggle in `AlbumNavPanel.vue`, browser zoom, breakpoint crossing) — a stale `itemsPerRow` would misplace every tile below the resize point. *Mitigation:* `ResizeObserver` on the scroll container plus `useBreakpoints()`'s own reactivity recompute `itemsPerRow`/`tileWidth` reactively (`albumTileWidth.ts`, DO-063-09); `useVirtualizer`'s own `getTotalSize()`/row math is a pure function of the current `itemsPerRow`, so a resize or breakpoint crossing is a cheap recompute, not a data re-fetch.
  - *Risk:* Sticky bucket headers (new UI behavior, not present today) could visually clash with `AlbumHero.vue`'s own sticky/hero elements or the existing sticky toolbar bug already flagged in `STUDY-MOBILE-v8.md` finding #10 (title text sliced by a sticky header on scroll). *Mitigation:* verify the new sticky bucket headers sit correctly relative to any existing sticky ancestor via a manual scroll-through pass on both mobile and desktop viewports before considering S-063-02 done; if the pre-existing #10 clipping bug reproduces with sticky bucket headers too, flag it as a follow-up rather than silently accepting a new visible defect.
  - *Risk (resolved, Q-063-10):* `AlbumThumb.vue`'s responsive tile width uses Tailwind breakpoint classes (`sm:w-[calc(25vw-1rem)]` etc., per `virtual-scrolling-study.md`) rather than a single JS-computed value. The original draft proposed a DOM-measured "probe tile" to avoid duplicating the breakpoint→formula mapping in JS; resolved instead in favor of the duplication, extracted into one shared lookup table both `AlbumThumbVirtual.vue`'s width classes and `albumTileWidth.ts`'s JS computation read from (FR-063-14) — the same table also carries each breakpoint's gap value (`AlbumThumbPanel.vue`'s `gap-1 sm:gap-2 md:gap-4`), not just tile width — avoids a probe tile's first-paint circularity and extra DOM node entirely, at the cost of one table to keep in sync if a breakpoint's `calc()`/gap value ever changes (mitigated by the single-shared-source structure: there's one table to update, not two independent copies). Tile *height* is never measured or duplicated — it's derived analytically from the computed width and the parent's own known `album_thumb_css_aspect_ratio` (FR-063-14), which carries no drift risk of its own.
  - *Risk:* Guest/anonymous browsing (no `UserState.user`) must not throw when FR-063-04's formula reads `current_user.id`/`current_user.may_upload`. *Mitigation:* `useUserStore().isGuest`/`user?.id` already returns a defined (non-throwing) shape for a guest per `UserState.ts`'s existing getters; the formula's owner-branch naturally evaluates to `false` when there's no id to match, verified by S-063-09.
  - *Risk:* `ThumbAssetService`'s shared cache is capped at 6,000 entries — below this feature's own 7,000+-child scale target, if every distinct cover were acquired at once. *Mitigation:* not a new risk this feature introduces — the cache is already ref-counted and only evicts entries with `refCount <= 0`; virtualization's own mount/unmount cycle keeps the number of *simultaneously mounted* (non-zero-refCount) tiles far below 6,000 at all times (NFR-063-03), so eviction only ever targets already-scrolled-past tiles. No change to `ThumbAssetService`'s capacity is needed or in scope.
  - *Risk:* `date_format_album_thumb`/`date_format_hero_created_at`-style configs are free-text (`type_range: STRING_REQ`, not an enum) — an admin can type any valid PHP `date()` format string, so `phpDateFormat.ts` (FR-063-16) must handle the general case, not a fixed handful of tokens. The adapted community reference implementation covers the common character set (`j d l D w m n F M Y y H h g a i s c`, `\`-escaped literals) but PHP's `date()` supports roughly 40 characters total. *Mitigation:* NFR-063-08's cross-check scenario exercises a representative set, not the full PHP character table; any character `phpDateFormat.ts` doesn't recognize should pass through literally (matching PHP's own behavior for an unrecognized character) rather than throwing — explicit implementation note for T-063-xx.
  - *Risk (resolved during implementation):* This project's `3xl`/`4xl` Tailwind breakpoint pixel thresholds weren't found in any `@theme` block during spec-drafting-time investigation. *Resolution:* grepped the actual compiled production CSS bundle (`public/build/assets/app-v8-*.css`) and found zero `3xl:`/`4xl:`-prefixed rules anywhere — confirming these are genuinely dead, pre-existing classes (Tailwind v4 silently drops any `3xl:`/`4xl:` utility when no matching `--breakpoint-3xl`/`--breakpoint-4xl` custom property is defined), affecting ~60 files project-wide, unrelated to this feature. `useBreakpoints(breakpointsTailwind)`'s default `sm`/`md`/`lg`/`xl`/`2xl` thresholds are therefore sufficient with no extension needed — `2xl`'s formula is what already applies at `2xl` and beyond.

## Implementation Drift Gate

Run the Analysis Gate checklist (`docs/specs/5-operations/analysis-gate-checklist.md`) once this plan and `tasks.md` exist, before I1 begins. Record the outcome under "Analysis Gate" below. Run the Implementation Drift Gate section of the same checklist once all tasks are `[x]` and record findings before marking this feature Complete.

## Increment Map

1. **I1 – Store: tier 1+2 fetch, positional boundary walk, stale-response guard, cache**
   - _Goal:_ `AlbumState.ts` fetches buckets+children together when the flag is on and derives bucket-section boundaries via a positional walk over tier 1's `counts` — no join or sort, since Feature 061 FR-061-26 already guarantees tier 2's row order matches tier 1's bucket boundaries. Also: a stale-response guard so a superseded album's late response never overwrites the current one (FR-063-18), and caching so revisiting a recent album skips the round-trip (FR-063-19).
   - _Preconditions:_ Spec approved (this plan committed); Feature 061 (incl. FR-061-26/27) ships tier 1/2 ordered with `is_pinned`/`is_public`/`is_link_required`.
   - _Steps:_ New actions (`loadAlbumsV3()`, `loadAlbumsAuto()`) + state/getters (`bucketsV3`/`boundariesV3`/`bucketableV3`) on `AlbumState.ts` (singular); adapted children written into `AlbumsState.ts` (plural)'s existing `albums` field; the boundary-walk logic itself factored into a small pure function (`counts[] + flatChildren[] → {bucketId, label, startIndex, count}[]`), including the defensive `sum(counts) !== children.length` fallback; FR-063-18's guard reuses the store's existing `requestedAlbumId`-capture-and-compare pattern (`loadHead()`/`loadAlbums()`/`loadPhotos()`/`load()` already use it) rather than a new generation counter; FR-063-19's caching reuses this codebase's existing `axios-cache-interceptor` convention via a new `AlbumChildrenV3Service` rather than a bespoke in-store `Map` cache; manual verification against Feature 061's existing per-source fixture parents (created_at/min_taken_at/max_taken_at/title-date_prefix/title-alphabetical/owner_id).
   - _Commands:_ `npm run check`.
   - _Exit:_ FR-063-01/02/18/19 satisfied; S-063-02/04/25/26 pass manually.

2. **I2 – Store: background rights fetch + client-side combination**
   - _Goal:_ Tier 3 fetched immediately after tier 1+2 resolve; combination formula applied reactively.
   - _Preconditions:_ I1 merged.
   - _Steps:_ New action (`loadAlbumsV3Rights()`) + reactive merge on `AlbumState.ts`/`AlbumsState.ts`; combination formula implemented as a small pure function (`combineAlbumChildRights()`, gated by `isRegularAlbumParent` per FR-063-05), manually cross-checked against Feature 061's existing rights fixtures (individually-shared child, parent-level delete grant, multi-group overlap, admin, guest, and a Tag/Person-parent fixture confirming the owner shortcut stays off).
   - _Commands:_ `npm run check`.
   - _Exit:_ FR-063-03/04/05 satisfied; NFR-063-04 verified; S-063-05..09/16 pass manually.

3. **I3 – Adapter: joined data → `ThumbAlbumResource` shape, plus the new tile component**
   - _Goal:_ `contextMenu.ts` renders/behaves identically fed by either data source — including the previously-broken Pin/Unpin label and public/hidden badges (Q-063-05/06) — and a working per-child tile component exists (`AlbumThumbVirtual.vue`) that resolves its own cover image via the existing Feature 056 Asset endpoint, since tier 2 gives it no pre-built URL to consume.
   - _Preconditions:_ I2 merged; Feature 061 FR-061-27 shipped (`is_pinned`/`is_public`/`is_link_required` on tier 2); I13 merged (`phpDateFormat.ts`, for `formatted_min_max`).
   - _Steps:_ Adapter function — metadata/rights, `is_pinned`/`is_public`/`is_link_required` mapped straight through from tier 2, `formatted_min_max` computed via I13's `phpDateFormat.ts`, `thumb` left `null` (FR-063-06); new `AlbumThumbVirtual.vue` (DO-063-06) forking `AlbumThumb.vue`'s chrome — `AlbumThumbOverlay.vue`/`AlbumThumbDecorations.vue`/`ThumbBadge.vue` reused unchanged, `AlbumThumbImage.vue` replaced with `<Thumb :album-id="child.id" :photo-id="child.cover_id" type="thumb">` — plus the no-cover fallback (password icon vs. generic no-image icon) from `cover_id`/`is_password_required`; manual side-by-side comparison, flag on vs. off, same fixture album, including a mixed-cover-state fixture (real cover, `null` + password-protected, `null` + not) and the pinned/public fixture (S-061-44).
   - _Commands:_ `npm run check`.
   - _Exit:_ FR-063-06/15 satisfied; NFR-063-02 verified; S-063-18/20/21/22/23 pass manually.

4. **I4 – Shared composable: row flattening + tile geometry**
   - _Goal:_ Pure, reusable `virtualAlbumRows.ts` (DO-063-02) — the one piece both virtualized renderers and the reimplemented drag-select depend on.
   - _Preconditions:_ I1 merged (needs bucket-boundary metadata's real shape).
   - _Steps:_ Implement `(children[], bucketMeta[], itemsPerRow, tileWidth, aspectRatioNumber) → {rows, geometryLookup}`, with each tile row's height computed as `tileWidth ÷ aspectRatioNumber` (FR-063-14); manually verify against a small hand-computed fixture (2 buckets, uneven counts, non-multiple-of-itemsPerRow, at least two different aspect ratios) before wiring into any renderer.
   - _Commands:_ `npm run check`.
   - _Exit:_ DO-063-02 ready for consumption by I5/I6/I8.

5. **I5 – Grid virtualization**
   - _Goal:_ `AlbumThumbGridVirtual.vue` (DO-063-03), wired into `AlbumPanel.vue` in place of `AlbumThumbPanel.vue`+`<Pagination>` when the flag is on (via a small `AlbumThumbPanelVirtual.vue` grid/list dispatcher), rendering I3's `AlbumThumbVirtual.vue` per child.
   - _Preconditions:_ I3, I4 merged.
   - _Steps:_ `useWindowVirtualizer` (page/window scroll — `AlbumPanel.vue`'s content is not itself a bounded, internally-scrolling box) over I4's flattened rows with a reactive `scrollMargin`; real header rows render as ordinary (non-sticky) rows, with a separate `position: sticky` overlay element showing the current section's label once its own header has scrolled past the top (`position: sticky` does not apply to the `translate3d`-positioned rows themselves); tile rows rendering `AlbumThumbVirtual.vue`; `useElementSize()`-driven `itemsPerRow` (grid's own root width measured, not duplicated — see plan Risks; height computed analytically from that measured width and the parent's `album_thumb_css_aspect_ratio`, FR-063-14 — never measured); manual DevTools element-count check against a 7,000+-child fixture; manual row-height check across all six aspect-ratio values (S-063-17); manual DevTools Network-panel check confirming cover-image requests fire only for mounted tiles and are released on scroll-out (S-063-19).
   - _Commands:_ `npm run check`.
   - _Exit:_ FR-063-07/08/09/14/15 satisfied; NFR-063-03 verified; S-063-02/03/04/17/19 pass manually.

6. **I6 – List-view virtualization**
   - _Goal:_ `AlbumListViewVirtual.vue` (DO-063-04), degenerate `itemsPerRow = 1` reuse of I4/I5's mechanism, rendering a new `AlbumListItemVirtual.vue` (DO-063-07, forked from `AlbumListItem.vue` the same way `AlbumThumbVirtual.vue` forks `AlbumThumb.vue`) per row.
   - _Preconditions:_ I5 merged.
   - _Steps:_ Thin wrapper reusing I4's composable; `AlbumListItemVirtual.vue` reuses `ListBadge.vue`/layout logic unchanged, replaces `AlbumThumbImage.vue` with `<Thumb>` at the fixed row thumbnail size; manual verification, flag on, `album_view_mode: 'list'`, including the mixed-cover-state fixture (S-063-24).
   - _Commands:_ `npm run check`.
   - _Exit:_ FR-063-10/17 satisfied; S-063-11/24 pass manually.

7. **I7 – Pagination control removal for the flag-on path**
   - _Goal:_ Hide `<Pagination>` for the subalbum section when the flag is on.
   - _Preconditions:_ I5 merged.
   - _Steps:_ Conditional in `AlbumPanel.vue`; manual verification both flag states.
   - _Commands:_ `npm run check`.
   - _Exit:_ FR-063-12 satisfied.

8. **I8 – Drag-select against precomputed geometry**
   - _Goal:_ Fix, not just document, the DOM-query gap for the album-selection branch.
   - _Preconditions:_ I4, I5 merged.
   - _Steps:_ New geometry-intersection branch in `dragAndSelect.ts`, gated to flag-on + virtualized panel; manual verification first against today's DOM-query branch's own existing correct cases (parity baseline), then against the unmounted-tile case DOM-query cannot handle (drag spanning above/below viewport in a large fixture).
   - _Commands:_ `npm run check`.
   - _Exit:_ FR-063-11 satisfied; S-063-12 passes manually.

9. **I9 – Mutation re-fetch wiring**
   - _Goal:_ Tier 1+2+3 re-fetch — and FR-063-19's per-album cache invalidation — after in-panel mutations (FR-063-13), including cover-photo changes now that tile rendering depends on `cover_id` (Q-063-09).
   - _Preconditions:_ I1, I2 merged.
   - _Steps:_ Wire the new store action alongside each existing `AlbumService.clearCache()` call site relevant to this section's own mutations; audit for an existing cover-change call site (manual cover selection, `RecomputeAlbumStatsJob` completion) or add one if none exists; manual verification per mutation type including cover change, confirming scroll-position stability (NFR-063-05).
   - _Commands:_ `npm run check`.
   - _Exit:_ FR-063-13 satisfied; NFR-063-05 verified; S-063-13/14 pass manually.

10. **I10 – TagAlbum/PersonAlbum parity pass**
    - _Goal:_ Confirm the full pipeline (I1–I9) behaves correctly when `album_id` resolves to a `TagAlbum`/`PersonAlbum`.
    - _Preconditions:_ I1–I9 merged.
    - _Steps:_ Manual verification against a Tag/Person listing fixture — instance-wide default order (Feature 061 FR-061-26), including the always-`false` delete/move check (FR-063-05).
    - _Commands:_ `npm run check`.
    - _Exit:_ S-063-10 passes manually.

11. **I11 – Accessibility pass**
    - _Goal:_ `aria-posinset`/`aria-setsize` (or equivalent) reflect true position/total, not just the mounted subset.
    - _Preconditions:_ I5/I6 merged.
    - _Steps:_ Add attributes to virtualized tile/row wrappers; manual screen-reader or DOM-inspection spot check.
    - _Commands:_ N/A (manual).
    - _Exit:_ NFR-063-07 verified.

12. **I12 – Documentation**
    - _Goal:_ Satisfy Documentation Deliverables.
    - _Preconditions:_ I1–I11, I13 merged.
    - _Steps:_ Update `api-design.md`, `knowledge-map.md`, `roadmap.md`.
    - _Commands:_ N/A (docs only).
    - _Exit:_ All Documentation Deliverables checked off.

13. **I13 – Client-side date formatting (`phpDateFormat.ts`)**
    - _Goal:_ `formatted_min_max`'s tile-subtitle text matches flag-off (server-formatted) rendering exactly, for any admin-configured `date_format_album_thumb`/`thumb_min_max_order` value — an independent piece I3's adapter depends on.
    - _Preconditions:_ Spec approved.
    - _Steps:_ Add `date_format_album_thumb`/`thumb_min_max_order` to `App\Http\Resources\GalleryConfigs\AlbumConfig`/`RootConfig` (this feature's own small backend addition — not a Feature 061 change, these are pre-existing general config resources already read from for `album_thumb_css_aspect_ratio`; regenerate TypeScript types); adapt the reference PHP-date-to-JS format-string conversion (community gist) into `phpDateFormat.ts` (DO-063-08); implement the min/max join+ordering logic mirroring `ThumbAlbumResource::formatMinMaxDate()`'s exact branching — including that "only one of `min_taken_at`/`max_taken_at` present" is *not* a collapse case, it stays `null` the same as "both absent" (FR-063-16); manual cross-check against real `date()` output (via a throwaway PHP script) for a representative format-string set, per NFR-063-08.
    - _Commands:_ `npm run check`; `make phpstan`; `vendor/bin/php-cs-fixer fix --dry-run` (for the small `AlbumConfig`/`RootConfig` PHP change).
    - _Exit:_ FR-063-16 satisfied; NFR-063-08 verified; S-063-23 passes manually.

14. **I14 – Smart-album root tiles (2026-09-02 addendum, Q-063-14/15)**
    - _Goal:_ Root gallery's Smart Albums section sources `baseSmartAlbums` from `GET /Albums/smart` when the flag is on, with real cover pixels, side by side with still-v2-sourced tag/person tiles in the same panel.
    - _Preconditions:_ Spec addendum approved; independent of I1–I13 (touches root-listing `AlbumsState.ts`, not the subalbum-grid `AlbumState.ts` those increments extend).
    - _Steps:_ Backend first — (a) `AlbumCategoryController::smart()` (Feature 062): batch `AlbumUserThumb::whereIn('album_id', $ids)->where('user_id', Auth::id())->pluck('photo_id', 'album_id')`, populate `cover_ids[]` from it (`null` on cache miss), FR-062-16; (b) `GetPhotoAssetRequest::isPhotoOfAlbum()` (Feature 056): add a `BaseSmartAlbum` branch mirroring the existing `TagAlbum`/`PersonAlbum` `isComputedAlbumThumb()` check, FR-056-08 — **plus** a corollary fix found only once this branch was actually exercised: `rules()` validated `album_id` via `RandomIDRule` (real-Album/TagAlbum/PersonAlbum-id-shaped only) instead of `AlbumIDRule` (the rule `GetAlbumRequest`/`GetAlbumHeadRequest` already use, which also accepts a `SmartAlbumType` value), so every smart-album request 422'd before ever reaching `isPhotoOfAlbum()` — swapped to `AlbumIDRule`. Then frontend — (c) new `resources/js/services/album-category-v3-service.ts` (DO-063-10); (d) new `resources/js/v8/utils/adaptSmartAlbumTile.ts` (DO-063-11, FR-063-21); (e) `AlbumsState.ts`: new `loadSmartAlbumsV3()` action, dispatched from `load()` when `is_struct_of_array_enabled` (FR-063-20); (f) `AlbumThumbImage.vue`/`AlbumThumb.vue`/`AlbumListItem.vue` (list view, an additional caller found during implementation): new `albumId`/`coverId` props and `<Thumb>` fallback branch (FR-063-22); (g) `AlbumService.clearAlbums()` — not `clearCache()` as originally drafted, corrected once the actual root-listing invalidation call site was identified: extend with the new `albums_v3_smart` cache id (FR-063-23).
    - _Commands:_ `npm run check`; `npm run format`; `make phpstan`; `vendor/bin/php-cs-fixer fix --dry-run`; `php artisan test --filter=AlbumCategoryV3Test` (16/16 green, includes a new cover-resolution test); `php artisan test --filter=PhotoAssetV3Test` (29/29 green, includes 3 new smart-album tests); `php artisan test --filter="Album.*V3Test"` (116/116 green, full regression sweep).
    - _Exit:_ FR-063-20..23 satisfied; NFR-063-09 verified (`git diff` shows no other category endpoint adopted); S-063-27..32 — manual browser verification not performed this session (no dev server/database available), same standing limitation as I10/I12 elsewhere in this plan.
    - _Superseded note (2026-09-03, I15):_ `adaptSmartAlbumTile.ts` (d above) was renamed/generalized to `adaptCategoryTile.ts` as part of I15 — every other file/task listed above is unchanged. NFR-063-09's "no other category endpoint adopted" exit criterion no longer holds — see I15.

15. **I15 – Full root-gallery SoA scope (2026-09-03 addendum, Q-063-16/17)**
    - _Goal:_ Adopt the four remaining Feature 062 endpoints (`/Albums/tags[/rights]`, `/Albums/persons?scope=`, `/Albums/pinned?scope=`, `/Albums/root[/buckets|/rights]?scope=`) so the entire root gallery page is Struct-of-Array-sourced when the flag is on — the user's clarified actual intent behind this whole addendum.
    - _Preconditions:_ I14 merged (reuses its `AlbumCategoryV3Service`/`adaptCategoryTile.ts`/store-wiring pattern); independent of I1–I13.
    - _Steps:_ (a) `AlbumCategoryV3Service`: add `getTags()`/`getTagsRights()`/`getPersons(scope)`/`getPinned(scope)`/`getRootBuckets(scope)`/`getRootChildren(scope)`/`getRootRights(scope)` (DO-063-10). (b) `adaptCategoryTile.ts`: generalize the smart-only adapter with a `kind` parameter (`is_tag_album`/`is_person_album`/`is_pinned`) and add `combineTagAlbumRights()` (DO-063-11, FR-063-25). (c) `adaptAlbumChildTile.ts`: refactor `combineAlbumChildRights()` to take `isOwner: boolean` directly; extract the sub-album call site's original derivation as `isRegularAlbumParentOwner()` (DO-063-14, FR-063-28); update `AlbumState.ts`'s one call site accordingly. (d) `AlbumsState.ts`: new own/shared bucket-tier state + `loadTagAlbumsV3()`/`loadPersonAlbumsV3()`/`loadPinnedAlbumsV3()`/`loadRootAlbumsV3(scope)`/`loadRootAlbumsV3Rights(scope)`, all dispatched in parallel from `load()`'s existing v3 branch (DO-063-13, FR-063-24/25/26/27). (e) New `AlbumRootGridVirtual.vue`/`AlbumRootListViewVirtual.vue`/`AlbumRootPanelVirtual.vue`, root-scope forks of DO-063-03/04/dispatcher, `scope` prop, `header` prop for the own-scope title (DO-063-12, FR-063-24/29). (f) `Albums.vue`: swap the own/shared `<AlbumThumbPanel>` calls (both tabbed and non-tabbed template branches, 4 call sites) for `<AlbumRootPanelVirtual>` when the flag is on. (g) `dragAndSelect.ts`: generalize `getAlbumBoxesV3()` to iterate every `[data-album-grid-root]`, branching on a new `data-album-grid-scope` attribute the two forks stamp (FR-063-30). (h) Bug fixes found along the way: `RootConfig.php` — remove the stray `#[LiteralTypeScriptType('App.Enum.AspectRatioType')]` override on `album_thumb_css_aspect_ratio` (correct type is `AspectRatioCSSType`, matching `AlbumConfig.php`'s sibling property, no override needed); regenerate `lychee.d.ts`.
    - _Commands:_ `npm run check`; `npm run format`; `make phpstan`; `vendor/bin/php-cs-fixer fix --dry-run`; `php artisan test --filter="Album.*V3Test|PhotoAssetV3Test"` (150+ tests, all green — confirms the `RootConfig` type fix caused no regression, and `AlbumCategoryV3Test`'s existing zero-`photos`-query assertion still holds unmodified).
    - _Exit:_ FR-063-24..30 satisfied; NFR-063-09 updated to reflect full adoption; S-063-33..40 — manual browser verification not performed this session (no dev server/database available).

## Scenario Tracking

| Scenario ID | Increment / Task reference | Notes |
|-------------|---------------------------|-------|
| S-063-01 | I1–I12 (regression guard, exercised throughout) | Flag-off behavior untouched. |
| S-063-02 | I1, I5 | Bucket order/labels, tier 2's server-guaranteed row order. |
| S-063-03 | I5 | Bounded DOM node count at 7,000+-child scale. |
| S-063-04 | I1, I5 | `bucketable: false` fallback. |
| S-063-05 | I2 | Rights not-yet-loaded default. |
| S-063-06 | I2 | Rights loaded, correct combination. |
| S-063-07 | I2 | Individually-shared child. |
| S-063-08 | I2 | Owner branch. |
| S-063-09 | I2 | Guest branch. |
| S-063-10 | I10 | TagAlbum/PersonAlbum parity. |
| S-063-11 | I6 | List-view virtualization. |
| S-063-12 | I8 | Drag-select across unmounted tiles. |
| S-063-13 | I9 | Scroll-position stability across a mutation re-fetch. |
| S-063-14 | I9 | New subalbum appears correctly placed. |
| S-063-15 | I1–I12 | Flag toggle is a pure config change. |
| S-063-16 | I2 | Admin caller, zero explicit grants. |
| S-063-17 | I4, I5 | Row-height math correctness across all six aspect-ratio values. |
| S-063-18 | I3 | Cover-image rendering across mixed cover states (real, no-cover, no-cover + password). |
| S-063-19 | I5 | Cover-image requests fire lazily per mounted tile and release on scroll-out. |
| S-063-20 | I3 | Video-cover tile shows no play-icon overlay (accepted regression). |
| S-063-21 | I3 | Pin/Unpin menu label correct for an already-pinned subalbum. |
| S-063-22 | I3 | Public/hidden and public/visible tile badges correct. |
| S-063-23 | I13 | Date subtitle matches PHP `date()` output across format-string variants. |
| S-063-24 | I6 | List view cover images, mixed-cover-state fixture, grid parity. |
| S-063-25 | I1, I9 | Repeat-navigation cache hit/invalidation-on-mutation. |
| S-063-26 | I1 | Stale cross-album response discarded on rapid navigation. |
| S-063-27 | I14 | Smart Albums section sourced from `GET /Albums/smart`. |
| S-063-28 | I14 | Cached cover renders via Asset endpoint. |
| S-063-29 | I14 | Cache-miss tile shows generic no-image icon. |
| S-063-30 | I14 | Stale cached cover (live-condition mismatch) still resolves, not a 403. |
| S-063-31 | I14 | Smart (v3) and tag/person (v2) tiles render side by side, no discontinuity. |
| S-063-32 | I14 | Flag-off parity, unaffected. |
| S-063-33 | I15 | Own-albums section virtualizes at scale. |
| S-063-34 | I15 | Own + shared grids mount simultaneously, non-tabbed SHOW mode. |
| S-063-35 | I15 | Shared-scope sticky headers show real owner names (or "unknown" for guests). |
| S-063-36 | I15 | Root rights combination: own scope full rights, shared scope grants-only. |
| S-063-37 | I15 | Tag-album rights: explicit grant vs. no grant. |
| S-063-38 | I15 | Person/pinned tiles: correct display, accepted no-rights gap. |
| S-063-39 | I15 | Drag-select spanning both own and shared grids. |
| S-063-40 | I15 | Flag-off parity, every root section, unaffected. |

## Analysis Gate

_Not yet run — record the outcome here once `tasks.md` exists and before I1 begins._

## Exit Criteria

- All FR/NFR rows in `spec.md` verified per their Measurement column.
- `npm run format`/`npm run check` clean.
- DevTools element-count evidence attached to the Implementation Drift Gate report confirming NFR-063-03 against a 7,000+-child fixture.
- Manual rights-combination cross-check evidence (guest/owner/individually-shared/multi-group-overlap/admin) attached confirming NFR-063-04.
- Manual date-format cross-check evidence (`phpDateFormat.ts` vs. real PHP `date()` output, representative format-string set) attached confirming NFR-063-08.
- Documentation Deliverables (I12) complete.
- Implementation Drift Gate report recorded in this plan.
- **(2026-09-02 addendum)** FR-063-20..23/NFR-063-09 verified per Measurement; `AlbumCategoryV3Test`/Feature 056 request test extended for FR-062-16/FR-056-08 green; I14's Implementation Drift Gate slice recorded.
- **(2026-09-03 addendum)** FR-063-24..30 verified per Measurement; full `Album*V3Test`/`PhotoAssetV3Test` sweep (150+ tests) green; I15's Implementation Drift Gate slice recorded.

## Follow-ups / Backlog

- **Photo-grid virtualization** — the next phase per `virtual-scrolling-study.md`'s own recommended ordering (grid/square layouts next, justified/masonry last). Not started here.
- ~~Root-scope (`Albums.vue`) bucketing/virtualization~~ — **done, I15 (2026-09-03), Q-063-16.**
- ~~(2026-09-02 addendum) Remaining root-category adoption~~ — **done, I15.**
- **(2026-09-03 addendum) `is_public`/`is_link_required`/etc. for the four flat category listings** — `smart`/`tags`/`persons`/`pinned` tiles hardcode these `false` (`adaptCategoryTile.ts`); a caller who has e.g. explicitly made a *pinned* album public would not see the public badge on its root tile when the flag is on. `AlbumCategoryListResource` would need extending (a Feature 062 change) to carry these fields the way `AlbumChildrenDataResource` already does for root's own/shared tiles. Low priority — accepted, documented gap, not blocking.
- **(2026-09-03 addendum) `SEPARATE_SHARED_ONLY` shared-albums-visibility mode vs. the new bucketed shared-root grid** — v2's client-side "exclude public albums owned by others" filter, applied on top of server-computed shared-root buckets, would desynchronize tier 1's bucket counts from the client-filtered tile list, tripping `computeBucketBoundaries()`'s count-mismatch fallback (flat, unbucketed rendering) whenever this mode is active and the filter actually removes a row. Not addressed this pass — one of several `shared_albums_visibility_mode` options, not the default. A real fix likely needs the filter expressed server-side (a new query parameter on `/Albums/root`) rather than client-side.
- **(2026-09-03 addendum) Collapse/expand toggle for the own/shared root section titles** — `AlbumRootPanelVirtual.vue`'s title (FR-063-29) is always-expanded, unlike `AlbumThumbPanel.vue`'s `UCollapsible`; revisit if users actually want to collapse a large own-albums section.
- **`bucket_id`-windowed tier-2 pagination** — if a single parent's whole-album-at-once tier-2 payload ever proves too large in practice, Feature 061's own Follow-ups already earmarks the fix (a `bucket_id` query param); revisit only with real payload numbers, not pre-emptively.
- **`dragAndSelect.ts`'s photo-selection branch** — left DOM-query-based (Non-Goals); revisit only if/when photo-grid virtualization (see above) makes that branch's own DOM-query assumption a real problem.
