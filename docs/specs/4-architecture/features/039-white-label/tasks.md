# Feature 039 Tasks – Lychee White Label

_Status: Ready for implementation (all questions resolved)_  
_Last updated: 2026-05-04_

> Keep this checklist aligned with the feature plan increments. Stage tests before implementation, record verification commands beside each task, and prefer bite-sized entries (≤90 minutes).
> **Mark tasks `[x]` immediately** after each one passes verification—do not batch completions. Update the roadmap status when all tasks are done.

## Checklist

### I1 – Database Migration

- [ ] T-039-01 – Create `white_label_enabled` config migration (FR-039-01).  
  _Intent:_ Insert `white_label_enabled` row in `configs` table with `cat='lychee SE'`, `value='0'`, `type_range='BOOL'`, `is_secret=1`, `level=1`, `order=3`. `down()` removes the row. Migration must be idempotent (`insertOrIgnore`).  
  _Verification commands:_  
  - `php artisan test`  
  - `make phpstan`  
  _Notes:_ Follow `BaseConfigMigration` pattern (see `database/migrations/2024_09_27_144741_add_supporter_fields.php`).

---

### I2 – InitConfig PHP Resource

- [ ] T-039-02 – Write Feature_v2 test: `GET /api/v2/Gallery/Init` returns `is_white_label_enabled = false` by default (FR-039-02).  
  _Intent:_ Failing test first — asserts new field is present in the Init payload.  
  _Verification commands:_  
  - `php artisan test --filter=InitConfigTest`  
  _Notes:_ Test must fail before T-039-03 is implemented.

- [ ] T-039-03 – Add `is_white_label_enabled` property to `InitConfig` (FR-039-02, NFR-039-04).  
  _Intent:_ Add `public bool $is_white_label_enabled;` to `app/Http/Resources/GalleryConfigs/InitConfig.php`; populate in the SE-setup section: `$this->is_white_label_enabled = $this->is_se_enabled && request()->configs()->getValueAsBool('white_label_enabled');` (mirrors the `is_live_metrics_enabled` pattern — Q-039-03 Option B).  
  _Verification commands:_  
  - `php artisan test`  
  - `make phpstan`  
  _Notes:_ TypeScript type is regenerated by the transformer; confirm `npm run check` also passes.

---

### I3 – Vue Front-End: Left Menu & Login Form

- [ ] T-039-05 – Add `is_white_label_enabled` to `LycheeState` store / confirm TypeScript transformer output.  
  _Intent:_ Ensure the reactive ref is available in Vue components after `InitConfig` adds the field.  
  _Verification commands:_  
  - `npm run check`  
  _Notes:_ May require running the TypeScript transformer artisan command; verify generated type file includes the new field.

- [ ] T-039-06 – Write Vitest unit test for `useLeftMenu`: "Lychee" section hidden when `is_white_label_enabled = true` (FR-039-03, S-039-02, UI-039-02).  
  _Intent:_ Failing test first.  
  _Verification commands:_  
  - `npm run check`  
  _Notes:_ Mock `lycheeStore` with `is_white_label_enabled = true`; assert all items in the "Lychee" group have `access = false` or the section is absent.

- [ ] T-039-07 – Update `leftMenu.ts`: gate "Lychee" section on `!is_white_label_enabled` (FR-039-03, S-039-02).  
  _Intent:_ Destructure `is_white_label_enabled` from `lycheeStore`; apply to the "Lychee" section items' `access` field.  
  _Verification commands:_  
  - `npm run check`  
  _Notes:_ Section filter already uses `.filter()` so setting `access = false` on all items causes the section to be dropped.

- [ ] T-039-08 – Write Vitest unit test for `LoginForm.vue`: branding `<div>` hidden when `is_white_label_enabled = true`, visible when `false` (FR-039-08, S-039-09, S-039-10, UI-039-09, UI-039-10).  
  _Intent:_ Failing test first. Cover both SE-enabled and SE-disabled sub-cases.  
  _Verification commands:_  
  - `npm run check`  
  _Notes:_ Mock `lycheeStore` with `is_white_label_enabled = true` / `false`; assert presence/absence of the `.text-muted-color.text-right` div.

- [ ] T-039-09 – Update `LoginForm.vue`: wrap branding `<div>` with `v-if="!is_white_label_enabled"` (FR-039-08, S-039-09, S-039-10).  
  _Intent:_ Add `is_white_label_enabled` to `storeToRefs(lycheeStore)` destructuring; wrap `<div class="text-muted-color text-right ...">` containing `Lychee <span v-if="is_se_enabled">SE</span>`.  
  _Verification commands:_  
  - `npm run check`  
  _Notes:_ The `<div>` sits inside `<template v-if="is_basic_auth_enabled">` so S-039-07 is handled automatically.

---

### I4 – Vue Front-End: Gallery Footer

- [ ] T-039-10 – Write Vitest unit test for `GalleryFooter.vue`: "Powered by Lychee" paragraph hidden when `is_white_label_enabled = true` (FR-039-04, S-039-02, UI-039-04).  
  _Intent:_ Failing test first.  
  _Verification commands:_  
  - `npm run check`

- [ ] T-039-11 – Update `GalleryFooter.vue`: wrap `<p class="hosted_by ...">` with `v-if="!is_white_label_enabled"` (FR-039-04).  
  _Intent:_ `GalleryFooter.vue` currently calls `InitService.fetchFooter()`; `is_white_label_enabled` must come from `lycheeStore` (already loaded on mount). Import `useLycheeStateStore` if not already imported.  
  _Verification commands:_  
  - `npm run check`

---

### I5 – Blade: Footer, Meta, Warning

> **Gate:** Resolve Q-039-02 (Blade config access mechanism) before starting.

- [ ] T-039-12 – Write PHPUnit test for `footer.blade.php`: "Powered by Lychee" hidden when `white_label_enabled = 1` (FR-039-05, S-039-02, UI-039-04).  
  _Intent:_ Failing test first — render blade component with config overridden.  
  _Verification commands:_  
  - `php artisan test`

- [ ] T-039-13 – Update `resources/views/includes/footer.blade.php`: wrap `<p class="hosted_by">` with Blade conditional on `white_label_enabled` (FR-039-05).  
  _Verification commands:_  
  - `php artisan test`

- [ ] T-039-14 – Write PHPUnit test for `meta.blade.php`: `<meta name="generator">` absent when `white_label_enabled = 1` (FR-039-06, S-039-02, UI-039-06).  
  _Intent:_ Failing test first.  
  _Verification commands:_  
  - `php artisan test`

- [ ] T-039-15 – Update `resources/views/components/meta.blade.php`: wrap `<meta name="generator" content="Lychee v7">` with Blade conditional (FR-039-06).  
  _Verification commands:_  
  - `php artisan test`

- [ ] T-039-16 – Write PHPUnit test for `warning-misconfiguration.blade.php`: generic placeholders appear when `white_label_enabled = 1` (FR-039-07, S-039-02, UI-039-07, UI-039-08).  
  _Intent:_ Failing test first. Assert both the `<h1>` text and the example URL.  
  _Verification commands:_  
  - `php artisan test`

- [ ] T-039-17 – Update `resources/views/components/warning-misconfiguration.blade.php`: replace "Lychee" → "your-application" and "lychee.example.com" → "your-application.example.com" under Blade conditional (FR-039-07).  
  _Verification commands:_  
  - `php artisan test`

---

### I6 – Translation Keys

- [ ] T-039-18 – Add `white_label_enabled` translation key to `lang/en/settings.php` and all 21 remaining locale files.  
  _Intent:_ Key under `lychee_se` group: `'white_label_enabled' => 'Hide Lychee branding (white label mode)'`. Locales other than `en` may use the English string as a placeholder.  
  _Verification commands:_  
  - `php artisan test`  
  _Notes:_ 22 locale files total: ar, bg, cz, de, el, en, es, fa, fr, hu, it, ja, nl, no, pl, pt, ru, sk, sv, tr, vi, zh_CN, zh_TW (check `lang/` directory for exact list).

---

### Documentation

- [ ] T-039-19 – Update `docs/specs/4-architecture/roadmap.md` to add Feature 039 entry.

- [ ] T-039-20 – Update `docs/specs/4-architecture/knowledge-map.md`: record `white_label_enabled` config key under the SE config group.

## Notes / TODOs

- **Q-039-01 (open):** If the operator wants a configurable brand name instead of "your-application", T-039-17 will need to be revised. Do not start T-039-17 until Q-039-01 is resolved.
- **Q-039-02 (open):** The exact mechanism to read the `white_label_enabled` config value in Blade views must be confirmed before T-039-12 – T-039-17. Look at how `footer.blade.php` and `meta.blade.php` currently read other config values to determine the correct helper call.
- **Q-039-03 (open):** Whether to gate `is_white_label_enabled` on SE being active at runtime is deferred. Current spec applies the toggle regardless; revisit if Q-039-03 is resolved as "must require active SE".
- `vite/index.html` contains the same warning text as `warning-misconfiguration.blade.php` — this is the dev-only Vite entry point and is intentionally out of scope for this feature.
