# Development Roadmap

High-level planning document for Lychee features and architectural initiatives.

## Active Features

| Feature ID | Name | Status | Priority | Assignee | Started | Updated | Progress |
|------------|------|--------|----------|----------|---------|---------|----------|

## Paused Features

| Feature ID | Name | Status | Priority | Reason | Paused Date |
|------------|------|--------|----------|--------|-------------|

## Completed Features

| Feature ID | Name | Completed | Notes |
|------------|------|-----------|-------|
| 063 | Album Timeline Buckets Frontend Adoption | 2026-08-30 | Spec/plan/tasks drafted. Frontend adoption of Feature 061's three v3 endpoints (mirrors Feature 058's relationship to Feature 057) — virtualizes `AlbumPanel.vue`'s subalbum grid/list via new components under `.../albumModule/Virtualized/` (`@tanstack/vue-virtual`, generalizing `AlbumNavTree.vue`'s single-column pattern to a multi-column, sticky-bucket-header, aspect-ratio-driven-height model), implements the client-side owner/grant rights-combination logic 061 deliberately left unimplemented, and reimplements `dragAndSelect.ts`'s album-selection branch against precomputed tile geometry instead of DOM queries. Store derives bucket boundaries via a positional walk over tier 1's per-bucket counts against tier 2's already-ordered rows (FR-061-26) — no client-side join/sort. Gated by the existing `is_struct_of_array_enabled` flag; no v2 backend or v7 changes. Implementation not started. *(2026-09-02 addendum, I14: root gallery's Smart Albums section additionally adopts Feature 062's `GET /Albums/smart` — folded into this feature rather than a new number, per user direction — with real cover pixels via a small Feature 062/056 amendment (FR-062-16, FR-056-08). 2026-09-03 addendum, I15, Q-063-16: user clarified the actual intent was full root-gallery Struct-of-Array adoption ("the point of the extension is to support Struct-of-Array on the root gallery page"), not smart-albums-only — the remaining four Feature 062 endpoints are now also adopted. Own/shared root albums get the same virtualized/bucketed grid sub-album children have, via new root-scope forks of the Virtualized/ components (`AlbumRootGridVirtual.vue`/`AlbumRootListViewVirtual.vue`/`AlbumRootPanelVirtual.vue`); tags get real rights (`combineTagAlbumRights()`); persons/pinned are flat own+shared-merged swaps with an accepted no-rights-endpoint gap (Feature 062 NG4). `combineAlbumChildRights()` refactored to take a precomputed `isOwner`, since root's ownership partitioning happens at the SQL level, not via a per-response `owner_id` comparison. `dragAndSelect.ts`'s virtualized geometry reimplementation generalized to handle root's simultaneous own+shared grids. Two unrelated pre-existing bugs found and fixed: `GetPhotoAssetRequest` validated `album_id` with the wrong ID rule (blocking every smart-album Asset request); `RootConfig.php` had a stray TS-type override forcing the wrong generated type for `album_thumb_css_aspect_ratio`. `make phpstan`/`php-cs-fixer`/`npm run check`/`npm run format` all clean; full `Album*V3Test`/`PhotoAssetV3Test` sweep (150+ tests) green. Manual browser verification not performed (no dev server/database available this session). Two accepted, documented gaps: `is_public`/etc. not exposed by the four flat category listings; `SEPARATE_SHARED_ONLY` visibility mode's client-side filter not reconciled with server-computed shared-root buckets.)* |
| 061 | Album Timeline Bucket Aggregation | 2026-08-29 | All 38 tasks (T-061-01..37, incl. T-061-12a) implemented and `[x]`. Backend-contract-only (no v8 frontend consumer): three new `GET /api/v3/Albums/{album_id}/children*` endpoints, gated server-side by `modules.is_struct_of_array_enabled` (unlike Feature 058's frontend-only gating, since this is new backend surface). One new nullable `albums.bucket_id` column + composite `(parent_id, bucket_id)` index, populated at write time only — never a live `DATE_FORMAT`/per-row computation — by a new shared `App\Services\AlbumBucketComputer` (`resolveGranularity()`/`compute()`), reused identically by `RecomputeAlbumStatsJob` (extended), the new `RecomputeChildAlbumBucketsJob` (single bulk `upsert()`, dispatched from `AlbumController::updateAlbum()`'s write site via `wasChanged(['album_sorting_col','album_sorting_order','album_timeline'])`), and the new `lychee:recompute-album-buckets` backfill command (one chunked self-join query, zero `photos` queries — NFR-061-03). `OWNER_ID` is excluded from bucketing entirely (every direct child of one album always shares that album's owner, so it can never produce more than one bucket) — `bucket_id` is `null` there, uncomputed. New instance-wide `title_bucket_mode` (`date_prefix` default \| `alphabetical`) / `title_bucket_prefix_length` configs govern `TITLE`-sorted bucketing only, propagated to already-computed rows solely via the backfill command (no per-album trigger). `GET /children/buckets` (`AlbumBucketController`) is a plain index-served `GROUP BY bucket_id` (`EXPLAIN QUERY PLAN` confirmed against a 7,000-child fixture — `COVERING INDEX`, no table scan), plus read-time-computed `labels[i]` reusing `TimelineData::fromAlbum()`'s own `timeline_album_date_format_*` config keys (independent implementation, not a shared code path). `GET /children` (`AlbumChildrenDataController`) is one flat `toBase()` query, zero joins beyond `AlbumQueryPolicy::applyVisibilityFilter()`'s own baseline — needed widening `AlbumQueryPolicy::joinBaseAlbumOwnerId()`'s existing `base_albums` subselect by one column (`is_nsfw`) rather than adding a second join (verified against 41 existing v2 consumer tests, zero regressions); `cover_id` reuses `AlbumListController::resolveCoverId()` (widened to `public static`). `GET /children/rights` (`AlbumChildrenRightsController`) reuses `AlbumQueryPolicy::joinSubComputedAccessPermissions(full: true, user: $currentUser)` under a distinct join alias/prefix (avoiding a SQL alias collision with `applyVisibilityFilter()`'s own non-full join of the same subquery), `GROUP BY`+`MAX()`-aggregated per child — required, since that subquery has no internal `GROUP BY` in `full: true` mode and a caller in multiple groups with separate matching grants on one child would otherwise yield duplicate rows (verified against direct `AlbumPolicy::canEdit()`/`canDownload()` calls for the same child/caller); admin callers short-circuit to every right `true` without the join/`exists()` query ever running. All three cache through the existing `ManagedCacheService`/`CacheKeyProvider::albumChildrenTag()` — zero new listener wiring for two of the three, but the rights endpoint surfaced one genuine invalidation gap (FR-061-22): a grant change directly on the *queried album itself* (affecting `can_delete_children`) wasn't covered by the existing `AccessPermissionChanged` handler, which only ever evicted the changed album's *parent's* tag — fixed by one additional `forgetTag()` call in `ManagedCacheAlbumListingInvalidator::handleAccessPermissionChanged()`, unit-tested. New test files: `RecomputeAlbumStatsJobBucketTest`, `RecomputeChildAlbumBucketsJobTest`, `RecomputeAlbumBucketsCommandTest`, `AlbumSortingBucketDispatchTest`, `AlbumBucketsV3Test`/`AlbumChildrenDataV3Test`/`AlbumChildrenRightsV3Test` (97 tests total across the full feature, all green), plus additions to `CacheKeyProviderTest`/`ManagedCacheAlbumListingInvalidatorTest`. `make phpstan`: 0 errors across 2884 files. `php-cs-fixer`: clean. `docs/specs/3-reference/api-design.md`, `database-schema.md`, and `docs/specs/4-architecture/knowledge-map.md` updated. A separate frontend-adoption feature (mirrors Feature 058) is required before v2's `AlbumChildrenController`/`GetAlbumChildrenRequest`/`AlbumRepository::getChildrenPaginated()` can actually retire — untouched by this feature. **Follow-up (2026-08-29, T-061-38..44):** tiers 2/3 extended to also support `TagAlbum`/`PersonAlbum` (matching-albums listings), mirroring `AlbumChildrenController`'s v2 resolution — `AlbumRepository::queryMatchingAlbumsForTag()`/`queryMatchingAlbumsForPerson()` extracted (behavior-preserving) from the existing paginated methods and reused unpaginated/`toBase()`-queried; `can_delete_children`/`can_move_children` always `false` for these two types (no single shared parent's grants apply to a dynamically-matched, disparately-parented set). Tier 1 (buckets) intentionally stays regular-`Album`-only per user decision — bucketing has no single governing sort column/granularity to group a matching-albums result by. 30 new/extended tests, all green; zero regression against `AlbumMatchingAlbumsTest`/`AlbumRepositoryTest`. **Follow-up (2026-08-30, FR-061-26/T-061-45..47):** discovered while drafting Feature 063 that `GET .../children` had shipped with zero `ORDER BY` clause at all — fixed at the source rather than compensated for client-side: real-`Album` rows now order by `bucket_id` first (mirroring `AlbumBucketController`'s own clause exactly, "unknown" always last) then the parent's effective sort criterion; `TagAlbum`/`PersonAlbum` rows order by the instance-wide default. 2 new tests plus row-order assertions added to the 2 existing bucket-correlation tests; `make phpstan`/`php-cs-fixer` clean; zero regression across all three tier test suites (48 tests). **Follow-up (2026-08-30, FR-061-27/T-061-48..50):** surfaced during Feature 063's ambiguity review that tier 2 never carried `is_pinned`/`is_public`/`is_link_required`, breaking the Pin/Unpin context-menu label and public/hidden tile badges once adopted frontend-side — added all three: `is_pinned` widened onto `AlbumQueryPolicy::joinBaseAlbumOwnerId()`'s existing `base_albums` subselect (zero extra join, same pattern as `is_nsfw`); `is_public`/`is_link_required` resolved via a new narrow-column `public_access_permissions` subquery join (pre-filtered to `user_id IS NULL AND user_group_id IS NULL`) — a raw table join was tried first and rejected mid-implementation when `access_permissions`' own `created_at` column collided with `SortingDecorator`'s unqualified `ORDER BY created_at`, caught by the existing `testBucketIdCorrelatesExactlyWithBucketsEndpoint` test. 1 new test, 1 existing exact-JSON test updated for the 3 new fields; `make phpstan`/`php-cs-fixer` clean; `php artisan typescript:transform` regenerated `lychee.d.ts` (also picked up several other Feature 061 v3 types that had never actually been regenerated before now). |
| 062 | Root Album Listing Struct-of-Arrays | 2026-09-02 | All 32 tasks (T-062-00..32, incl. T-062-19a/23a/23b/23c) implemented and `[x]`. Ports Feature 061's buckets/index/rights SoA contract to root albums (`GET /api/v3/Albums/root[/buckets\|/rights]`, `scope=own\|shared`) plus four separate flat listings (`/Albums/smart`, `/Albums/persons?scope=`, `/Albums/tags[/rights]`, `/Albums/pinned?scope=`) — 8 new routes, all under a new `App\Http\Controllers\Gallery\AlbumListing` namespace. `own` scope reuses the existing date/title `bucket_id` mechanism verbatim, kept live against instance-wide config changes by a new `RecomputeRootAlbumBucketsJob` (dispatched from `SettingsController::setConfigs()` on a 5-key intersection: `sorting_albums_col`/`sorting_albums_order`/`timeline_albums_granularity`/`title_bucket_mode`/`title_bucket_prefix_length`). `shared` scope groups by owner via a **live** `GROUP BY base_albums.owner_id` at read time — never via the persisted `bucket_id` column, which stays exclusively date/title-derived everywhere, confirmed by a dedicated cross-endpoint correlation test. Bucket labels are authentication-gated: an authenticated caller gets `COALESCE(display_name, username)` via one `LEFT JOIN users`; a guest never executes that join, every label hardcoded `"unknown"` — verified via query-log assertion, not just response content. `bucketable` is unconditionally `true` for `shared` scope, even on a zero-result query. `/Albums/persons`/`/Albums/pinned` gained the identical `scope=own\|shared` split (one shared `GetScopedAlbumsRequest` now serving 5 methods across 2 controllers) but their `shared` scope is deliberately flat/ungrouped — neither has a `/buckets` route, confirmed by tests asserting the wildcard `{album_id}/buckets` fallthrough 422s rather than 200s. `/Albums/root/rights`'s `owner_id` field is omitted from the JSON payload entirely (`Spatie\LaravelData\Optional`, not a serialized `null`) for both scopes — root has no single owner to report. A second, independent change deletes `ColumnSortingAlbumType::OWNER_ID` outright (dead since Feature 060's dropdown narrowing, never actually removed from the enum) via a one-way migration (`2026_09_02_120000_remove_owner_id_album_sorting.php`) rewriting surviving `owner_id` values to `created_at`; `ColumnSortingType::OWNER_ID` (the broader internal enum, used for the live `shared`-scope ordering) is untouched. Feature 061's 3 single-action controllers (`AlbumBucketController`/`AlbumChildrenDataController`/`AlbumChildrenRightsController`) are consolidated into one `AlbumListing\AlbumChildrenController`, and its already-shipped `/Albums/{album_id}/children[/buckets\|/rights]` routes are renamed to `/Albums/{album_id}[/buckets\|/rights]` (confirmed safe — zero v8 frontend references to the old path) — its existing 3 test files (`AlbumBucketsV3Test`/`AlbumChildrenDataV3Test`/`AlbumChildrenRightsV3Test`, 55 tests) pass with only request-URL-literal edits, zero assertion changes, proving the consolidation is behavior-preserving. File-count ceiling (NFR-062-09) met exactly: 3 net controllers, 2 new FormRequests (`GetScopedAlbumsRequest`/`GetAlbumCategoryRequest`), 2 new Resources (`AlbumCategoryListResource`/`AlbumCategoryRightsResource`) plus 2 widened existing ones (`AlbumChildrenDataResource` gained `owner_ids[]`; `AlbumChildrenRightsResource`'s `owner_id` widened to `string|Optional`). NFR-062-05 (own∪shared reconstructs v2's exact partition) verified via a dedicated fixture comparing `Top::get()`'s output against v3's scoped queries for both an authenticated user and a guest. New test files: `AlbumRootV3Test` (25 tests), `AlbumCategoryV3Test` (15 tests), `OwnerIdSortingMigrationTest` (5), `RecomputeRootAlbumBucketsJobTest` (6), plus extensions to `UpdateSettingsTest`, `RecomputeAlbumBucketsCommandTest`, `CacheKeyProviderTest`, and `ManagedCacheAlbumListingInvalidatorTest` (which now directly verifies FR-062-14's "no new invalidation listener wiring required" claim against the real new cache-key methods, not just the pre-existing generic ones). `make phpstan`: 0 errors across 2896 files. `php-cs-fixer`: clean. Full targeted regression (`--filter=Album*V3Test`, `--filter=Settings` — 30/31 green, one pre-existing unrelated ldap-extension failure —, `--filter=Cache` — 112/112 green —, `AlbumListV3Test`, `PatchTest`, `UpdateAlbumRequestTest`) all green, zero regressions. Implementation Drift Gate (`routes/api_v2.php`/`AlbumsController.php`/`Top.php`/`RootAlbumResource.php`/`resources/js/v7/**`) confirmed empty. `docs/specs/3-reference/api-design.md`, `database-schema.md`, and `docs/specs/4-architecture/knowledge-map.md` updated. Backend-only (NG2) — v8 frontend adoption is a follow-up feature, mirrors how Feature 063 adopted Feature 061. *(2026-09-02 amendment: `/Albums/smart`'s adoption ended up folded into Feature 063 as an addendum, not a separate feature number; that addendum also amends `AlbumCategoryController::smart()` with a real, cache-only `cover_ids` resolution, FR-062-16, T-062-33, planned not yet implemented.)* |
| 061 | Album Timeline Bucket Aggregation | 2026-08-29 | All 38 tasks (T-061-01..37, incl. T-061-12a) implemented and `[x]`. Backend-contract-only (no v8 frontend consumer): three new `GET /api/v3/Albums/{album_id}/children*` endpoints, gated server-side by `modules.is_struct_of_array_enabled` (unlike Feature 058's frontend-only gating, since this is new backend surface). One new nullable `albums.bucket_id` column + composite `(parent_id, bucket_id)` index, populated at write time only — never a live `DATE_FORMAT`/per-row computation — by a new shared `App\Services\AlbumBucketComputer` (`resolveGranularity()`/`compute()`), reused identically by `RecomputeAlbumStatsJob` (extended), the new `RecomputeChildAlbumBucketsJob` (single bulk `upsert()`, dispatched from `AlbumController::updateAlbum()`'s write site via `wasChanged(['album_sorting_col','album_sorting_order','album_timeline'])`), and the new `lychee:recompute-album-buckets` backfill command (one chunked self-join query, zero `photos` queries — NFR-061-03). `OWNER_ID` is excluded from bucketing entirely (every direct child of one album always shares that album's owner, so it can never produce more than one bucket) — `bucket_id` is `null` there, uncomputed. New instance-wide `title_bucket_mode` (`date_prefix` default \| `alphabetical`) / `title_bucket_prefix_length` configs govern `TITLE`-sorted bucketing only, propagated to already-computed rows solely via the backfill command (no per-album trigger). `GET /children/buckets` (`AlbumBucketController`) is a plain index-served `GROUP BY bucket_id` (`EXPLAIN QUERY PLAN` confirmed against a 7,000-child fixture — `COVERING INDEX`, no table scan), plus read-time-computed `labels[i]` reusing `TimelineData::fromAlbum()`'s own `timeline_album_date_format_*` config keys (independent implementation, not a shared code path). `GET /children` (`AlbumChildrenDataController`) is one flat `toBase()` query, zero joins beyond `AlbumQueryPolicy::applyVisibilityFilter()`'s own baseline — needed widening `AlbumQueryPolicy::joinBaseAlbumOwnerId()`'s existing `base_albums` subselect by one column (`is_nsfw`) rather than adding a second join (verified against 41 existing v2 consumer tests, zero regressions); `cover_id` reuses `AlbumListController::resolveCoverId()` (widened to `public static`). `GET /children/rights` (`AlbumChildrenRightsController`) reuses `AlbumQueryPolicy::joinSubComputedAccessPermissions(full: true, user: $currentUser)` under a distinct join alias/prefix (avoiding a SQL alias collision with `applyVisibilityFilter()`'s own non-full join of the same subquery), `GROUP BY`+`MAX()`-aggregated per child — required, since that subquery has no internal `GROUP BY` in `full: true` mode and a caller in multiple groups with separate matching grants on one child would otherwise yield duplicate rows (verified against direct `AlbumPolicy::canEdit()`/`canDownload()` calls for the same child/caller); admin callers short-circuit to every right `true` without the join/`exists()` query ever running. All three cache through the existing `ManagedCacheService`/`CacheKeyProvider::albumChildrenTag()` — zero new listener wiring for two of the three, but the rights endpoint surfaced one genuine invalidation gap (FR-061-22): a grant change directly on the *queried album itself* (affecting `can_delete_children`) wasn't covered by the existing `AccessPermissionChanged` handler, which only ever evicted the changed album's *parent's* tag — fixed by one additional `forgetTag()` call in `ManagedCacheAlbumListingInvalidator::handleAccessPermissionChanged()`, unit-tested. New test files: `RecomputeAlbumStatsJobBucketTest`, `RecomputeChildAlbumBucketsJobTest`, `RecomputeAlbumBucketsCommandTest`, `AlbumSortingBucketDispatchTest`, `AlbumBucketsV3Test`/`AlbumChildrenDataV3Test`/`AlbumChildrenRightsV3Test` (97 tests total across the full feature, all green), plus additions to `CacheKeyProviderTest`/`ManagedCacheAlbumListingInvalidatorTest`. `make phpstan`: 0 errors across 2884 files. `php-cs-fixer`: clean. `docs/specs/3-reference/api-design.md`, `database-schema.md`, and `docs/specs/4-architecture/knowledge-map.md` updated. A separate frontend-adoption feature (mirrors Feature 058) is required before v2's `AlbumChildrenController`/`GetAlbumChildrenRequest`/`AlbumRepository::getChildrenPaginated()` can actually retire — untouched by this feature. **Follow-up (2026-08-29, T-061-38..44):** tiers 2/3 extended to also support `TagAlbum`/`PersonAlbum` (matching-albums listings), mirroring `AlbumChildrenController`'s v2 resolution — `AlbumRepository::queryMatchingAlbumsForTag()`/`queryMatchingAlbumsForPerson()` extracted (behavior-preserving) from the existing paginated methods and reused unpaginated/`toBase()`-queried; `can_delete_children`/`can_move_children` always `false` for these two types (no single shared parent's grants apply to a dynamically-matched, disparately-parented set). Tier 1 (buckets) intentionally stays regular-`Album`-only per user decision — bucketing has no single governing sort column/granularity to group a matching-albums result by. 30 new/extended tests, all green; zero regression against `AlbumMatchingAlbumsTest`/`AlbumRepositoryTest`. |
| 060 | Database-Driven Title Sorting | 2026-08-28 | 34/35 tasks (T-060-01..31, 33..34) implemented and `[x]`; T-060-32 (manual browser dropdown screenshot) not performed this session — sandbox serves prebuilt `/dist` assets rather than live Vite HMR at the custom `APP_URL`, so `npm run check`'s full type-check plus code review (all dropdowns consume the shared, now-narrowed `constants.ts` arrays) stand in for it. Replaces PHP-level `SORT_NATURAL`/`SORT_FLAG_CASE` Title/Title-strict/Description/Description-strict sorting (photos + `base_albums`) with a pure SQL `ORDER BY` on two new derived columns, `title_base`/`title_index`, computed by a hardcoded 2-rule (+ extension-aware pre-strip) `App\Services\TitleSplitter::split()`. Description is no longer a sort criterion at all (still fully searchable/displayed). `TitleSplitter` is called **explicitly at all 12 write sites** — deliberately no Eloquent model event/hook, per explicit user direction — backstopped by a permanent `TitleSplitIntegrityTest`. Found and resolved one real spec self-contradiction during implementation (Q-060-04: FR-060-02's "extension discarded" wording conflicted with NFR-060-09's cross-extension-tie requirement; resolved by re-appending the extension to `base`) and one production join gap (`AlbumQueryPolicy::joinBaseAlbumOwnerId()`'s lightweight `base_albums` sub-select needed `title_base`/`title_index` added). `SortingDecorator::POSTPONE_COLUMNS`/`applyPhpSorting()` deleted entirely — fixes the documented pagination-reshuffle bug, since sorting is now 100% SQL. 5 relation `match()` methods (person/tag/recursive/child photos, child albums) simplified to order at the SQL layer instead of re-implementing natural sort in PHP. Config migration auto-rewrites `sorting_*_col` values/`type_range` for existing installs (idempotent). 23-locale `gallery.php` sweep removed 6 orphaned keys; `lychee.d.ts` regenerated. `make phpstan`: 0 errors across 2862 files. `php-cs-fixer`: clean. `npm run check`/`npm run format`: clean. `php artisan test` (unfiltered) hits the same pre-existing 600s process-timeout documented for Features 052/055/056; verified instead via targeted `--filter` runs covering every touched area — `TitleSplitterTest`, `PhotoTitleSyncTest`/`AlbumTitleSyncTest`, `TitleSplitIntegrityTest`, `TitleBackfillTest`, `TitleSorting` (Photo+Album), `SortingConfigMigrationTest`, `Sorting` (broad), `SmartAlbum` (88 tests), `Person` (180 tests), `Tag` (191 tests), `Album` (895 tests), `Renamer` (96 tests), `LangTest` — all green, zero regressions. |
| 059 | Embed Metadata in Original/RAW File | 2026-08-28 | All 16 tasks (T-059-01..16) implemented and `[x]`, except T-059-02 (manual browser check of the settings UI — not verified this session, no browser available; low risk since it's the existing generic `BoolField.vue` renderer). New opt-in `embed_metadata_in_files_enabled` config (off by default) + independent `embed_metadata_update_checksum_enabled` config (on by default), both category `Image Processing`. New `App\DTO\MetadataWritePayload`, `App\Metadata\Writer` (shells out to `exiftool` via `Illuminate\Support\Facades\Process::run()`, array-form only — NFR-059-01), `App\Jobs\EmbedMetadataJob` (mirrors `WatermarkerJob`'s `ShouldQueue`/`ShouldBeUnique`/`JobHistory` shape; `handle()`'s entire body wrapped in one top-level try/catch per NFR-059-07, since the default `QUEUE_CONNECTION=sync` runs it inline and an escaped exception would break the triggering request). Dispatched from `PhotoController::update()`/`tags()`/`rate()`; the rating path is owner-gated via a plain `$user->id === $photo->owner_id` inline check (left `PhotoPolicy::isOwner()` untouched — it's `private` and trivial). Writes the Original and, when present, RAW size variant, each independently — either can fail without aborting the other (FR-059-09, symmetric). Non-local disks (S3) are skipped via a broadened `\Throwable` catch around `toLocalFile()` (a misconfigured S3 adapter throws a plain `TypeError`, not just `MediaFileOperationException`, discovered during testing). Post-write, `SizeVariant::filesize` is always refreshed; `Photo::checksum`/`original_checksum` only when `embed_metadata_update_checksum_enabled` is on. New tests: `tests/Unit/Metadata/WriterTest.php` (7 tests, exact-argument-array assertions via `Process::fake()`), `tests/Unit/Jobs/EmbedMetadataJobTest.php` (13 tests, real fake-disk-backed files so the checksum/filesize re-measure runs for real), plus dispatch-assertion extensions to `PhotoEditTest`/`PhotoTagsTest`/`PhotoRatingIntegrationTest` (`Bus::fake()`). Translations added to `lang/en/all_settings.php` and propagated (English placeholder, per Feature 054's precedent) to all 21 other locales; `LangTest` green. `make phpstan`: 0 errors across 2857 files. `php-cs-fixer`: clean on every touched `app`/`tests` file (lang files deliberately excluded — running the fixer against them reformats the whole file, tabs vs. the project's spaces convention there; discovered and reverted mid-session). Full `--testsuite=ImageProcessing` (232 tests) and `--testsuite=Feature_v2 --filter=Photo` (170 tests) both clean apart from confirmed pre-existing/unrelated issues: a missing `tracks` table (a different in-flight feature's pending migration in this shared sandbox DB, hit by any test that deletes an album) and the already-documented `PhotosAddHandlerImagickTest`/`PhotoAddTest` PDF/Apple-Live-Photo failures (see Feature 054's own session notes). |
| 058 | Album Listing v3 Adoption | 2026-08-22 | Spec/plan/tasks drafted, both open questions (Q-058-01..02) resolved. Feature 057 (its dependency) now implemented. Implementation not started. |
| 057 | Album Listing v3 | 2026-08-22 | All 12 tasks (T-057-01..12) implemented and `[x]`. First `/api/v3/...` endpoint to realize ADR-0009's Struct-of-Arrays collection convention: `GET /api/v3/Albums` (`AlbumListController`, `AlbumListV3Request`, `AlbumListResource`/`AlbumListBulkEditFieldsResource`), curated via `AlbumQueryPolicy::applyVisibilityFilter()` (visibility not reachability — a password-locked-but-visible album still appears, Q-057-01), queried via `toBase()` (no Eloquent hydration), never paginated. New `AlbumQueryPolicy::joinBaseAlbumBulkEditFields()` join helper (mirrors `joinBaseAlbumSensitive()`); `for_bulk_edit=true` additionally joins `users` (`COALESCE(display_name, username)` for `owner_name`, since raw `toBase()` rows can't use the `User::getNameAttribute()` accessor) and a second `joinSubComputedAccessPermissions(prefix: 'public_', user: null)` for the public permission row. `cover_ids` (FR-057-09, added mid-scoping per Q-057-05) resolved via a pure `resolveCoverId()` helper mirroring `HasAlbumThumb::getCoverTypeForAlbum()`'s priority rule, in every mode. `with_parent_id`/`for_bulk_edit` are independent, admin-gated, combinable booleans (Q-057-02). Cached via `ManagedCacheService::rememberIf()`, new `CacheKeyProvider::albumListingV3Key()`/`albumListingV3Tag()` (NFR-057-04 key-uniqueness unit-tested across the (guest, user A, user B) × 4-flag-combination matrix); `ManagedCacheAlbumListingInvalidator` extended so all 14 handlers also evict the new coarse `album-listing-v3` tag (FR-057-06, deliberately coarse — whole-cache eviction on any album mutation, favoring simplicity over precision). New `tests/Feature_v3/Album/AlbumListV3Test.php` (20 tests, ~1150 assertions) covers S-057-01..18; new `AlbumQueryPolicyTest`/`CacheKeyProviderTest` unit suites. `make phpstan`: 0 errors across 2826 files. `php-cs-fixer`: clean. `docs/specs/3-reference/api-design.md` and `docs/specs/4-architecture/knowledge-map.md` updated. |
| 056 | API v3 Asset Retrieval | 2026-08-21 | All 18 tasks (T-056-01a..18) implemented and `[x]`. First `/api/v3/...` endpoint (`GET /api/v3/Photo/{photo_id}/Asset/{size_variant}`, `routes/api_v3.php`, `PhotoAssetController`), coexisting with v2 unchanged (Implementation Drift Gate: empty diff on `routes/api_v2.php`/`routes/web_v2.php`/`SecurePathController.php`/`UrlGenerator.php`). New `GetPhotoAssetRequest` resolves `Photo`/`SizeVariant`, validates an optional paired `X-Timestamp`/`X-Mac` temporary-link signature (new stateless `TemporaryLinkSigner` service, HMAC-SHA256 of the timestamp keyed by `app.key`) ahead of `PhotoPolicy::CAN_SEE`/`CAN_ACCESS_FULL_PHOTO`, with a `signatureRequired()` predicate (ADR-0008, mirrors `UrlGenerator::shouldNotUseSignedUrl()`) deciding per-caller whether a session alone suffices; overrides `failedAuthorization()` to key 401-vs-403 off which check failed rather than session state. Watermark-aware (`Watermarker::get_path()`) and S3-redirect-aware (302 to a native temporary URL, mirrors `UrlGenerator::getAwsUrl()`'s `AwsS3V3Adapter` detection) file resolution. Discovered mid-implementation (Q-056-07): the shared `api` middleware group's JSON content-negotiation is incompatible with a binary-passthrough endpoint — this route opts out of `accept_content_type:json`/`content_type:json` and adds a new `json_errors` middleware (`EnsureJsonErrorResponses`) so error responses still render as Lychee's standard JSON body regardless of the caller's real `Accept` header. New `tests/Feature_v3/` test tree (`Feature_v3` phpunit testsuite) with its own `BaseApiWithDataTest` extending the v2 fixture graph by inheritance (zero v2 edits); `PhotoAssetV3Test` covers the full S-056-01..17 scenario matrix plus NFR-056-04's 2×2×2 config/caller-state matrix (18 tests, ~1000 assertions), all written test-first per increment. `make phpstan`: 0 errors across all 2822 files. `php-cs-fixer`: clean. `php artisan test`: full unfiltered run hits the same pre-existing environment-level process-timeout/SQLite-lock-contention issue documented for Features 052/055 (unrelated to this feature — confirmed by running an untouched existing test, `SecureImageLinksTest`, cleanly in isolation, and by a clean low-contention `--testsuite=Unit` run showing only one pre-existing unrelated failure, a missing php-ldap extension constant in `LdapServiceTest`); verified instead via the full `Feature_v3` suite, `TemporaryLinkSignerTest`, and targeted spot-checks of shared-file-adjacent tests (`LoginRequiredTest`, since `app/Http/Kernel.php` gained one new middleware alias), all green. `docs/specs/3-reference/api-design.md` and `docs/specs/4-architecture/knowledge-map.md` updated. *(2026-09-02 amendment: `GetPhotoAssetRequest::isPhotoOfAlbum()` gains a `BaseSmartAlbum` cache-exception branch, FR-056-08, T-056-19, a precondition for Feature 063's smart-album-root-tiles addendum — planned, not yet implemented.)* |
| 055 | Multi-Track Albums | 2026-08-18 | All 30 tasks (T-055-01..30) implemented and `[x]`. Replaces the single-nullable-column `albums.track_short_path` with a `tracks` child table (`app/Models/Track.php`, own auto-increment PK, `disk` cast to `StorageDiskType`, explicit `is_primary` boolean — `oldestOfMany`/`ofMany` dropped, zero prior usage anywhere in this codebase, Q-055-10) + backfill/drop-column migration (S-055-13). v7's single-track UI is fully unchanged (`resources/js/v7/` diff empty, NFR-055-01/S-055-15) — `Album::setTrack()`/`deleteTrack()` transparently delegate to the primary track, with explicit next-oldest promotion on delete. New v8-only `POST`/`PATCH`/`DELETE /Album::tracks` REST surface (`AlbumTracksController`, a new standalone controller per Q-055-08 — no prior precedent existed to mirror), `TrackResource`, `tracks[]` added to `HeadAlbumResource`/`PositionDataResource`. Fixed the pre-existing `Actions\Album\Delete` hardcoded-`StorageDiskType::LOCAL` gap (FR-055-12): tracks now collected recursively across the album subtree, grouped by disk, one `FileDeleterJob` per distinct disk. New `UploadTrackToS3Job`/`lychee:track_s3_migrate` mirror the existing `SizeVariant` S3-offload pattern. Frontend: forked `resources/js/v8/services/track-service.ts` (NFR-055-01, shared `album-service.ts` untouched), new `AlbumTracks.vue` section registered inside the existing Album Settings modal (no new dialog, Q-055-05), `Map.vue` rewritten for one `L.GPX` layer per track wired into Leaflet's native `L.control.layers` legend (Q-055-02, no bespoke component) — also fixed a pre-existing bug where `Map.vue` never rendered anything on a photo-less album (found while implementing S-055-14, documented in plan.md's Implementation Drift Gate). All 13 Q-055-* clarifications resolved (Q-055-01..05 initial, Q-055-06..13 from a same-day codebase-verification pass). `make phpstan`: 0 errors across all 2811 files. `php-cs-fixer`: clean. `npm run check`/`npm run format`: clean. `php artisan test`: full unfiltered run hits a pre-existing environment-level 600s process time limit (documented precedent from Feature 052, unrelated to this feature); verified instead via targeted runs — all new Track test files plus the full `--filter=Album` (812 tests) and `--filter=Delete` (129 tests) suites, zero failures. |
| 054 | Configurable Landing Page | 2026-08-11 | All 63 tasks (T-054-01..63, incl. T-054-15a) implemented and `[x]`. 6 new enums (`LandingLayoutType`, `LandingTextPosition`, `LandingAnimationPreset`, `LandingLinkPlacement`, `LandingFeaturedItemsMode`, `LandingFeaturedItemType`); 12 new scalar configs under `Mod Welcome` (a new `int:MIN:MAX` bounded-range `type_range` convention added to `Configs::sanity()`/`ConfigGroup.vue` for `landing_hero_text_opacity`/`landing_featured_items_count`); `LandingLink`/`LandingFeaturedItem` models+migrations+factories+full admin CRUD (incl. a new `{ ids: string[] }` full-list-resync `Reorder` endpoint pattern, no prior precedent in this codebase); `LandingPageResource` extended with SE-fallback layout/animation resolution (mirrors `InitConfig::set_supporter_properties`) and automatic/manual featured-content resolution (`LandingFeaturedContentResource`, `Photo`/`Album` unified projection). Frontend: `Landing.vue` is now a thin dispatcher over 4 prop-driven layout components (`LandingClassic`/`LandingPortfolio`/`LandingMinimal`/`LandingStudio`, all under `resources/js/v8/views/landing/`), `useLandingTextPosition`/`useLandingAnimation`/`useScrollReveal` composables (the latter is `parallax_scroll`'s `IntersectionObserver`-driven per-section reveal), new `landingZoomReveal`/`landingSlideReveal` CSS keyframes. New admin page `LandingConfig.vue` (Settings tab with WatermarkPreview-style local-draft-then-explicit-Save plus a live scaled-down preview reusing the real layout components; Links and Featured tabs with immediate-save CRUD and native-HTML5-DnD drag-reorder — no drag library existed in this repo, none added). Q-054-01 resolved (`ConfigIntegrity` whitelist deliberately *not* touched — see open-questions.md). `resources/js/v7/` diff confirmed empty (NFR-054-08). `php artisan test`: full suite green except pre-existing unrelated failures (confirmed via file paths outside this feature — `OptimizeTablesTest`, `PhotosAddHandlerImagickTest`, `PhotoAddTest` apple-live-photo cases). `make phpstan`: 0 errors. `npm run check`/`npm run format`: clean. 22-locale translation sweep done (English-placeholder convention for untranslated new keys, matching existing repo practice); `LangTest`/`CopyrightTest` both green. |
| 053 | Album Listing Caching | 2026-08-10 | All 24 tasks (T-053-01..24) implemented and green via targeted `--filter` test runs (full-suite run deferred per explicit instruction for this session). Resumes Feature 052's deferred/superseded invalidation design for the album-listing half only. Six independently-cached SQL queries across three consumers — `AlbumRepository::getChildrenPaginated()`, each of `Actions\Albums\Top::get()`'s four constituent queries (tag/person/pinned/root albums, each with its own type-discriminating key prefix per NFR-053-08), and `GetTagWithPhotosAndAlbums::getAccessibleAlbums()` (session-unlock-state-aware key per NFR-053-07) — all via new `ManagedCacheService::rememberIf()`. 9 new domain events, 20 new/fixed dispatch sites (incl. the `SetProtectionPolicy` `TypeError` fix for tag/person albums, FR-053-11, plus two more latent instances of the same bug class found in `AlbumController::rename()`/`setPinned()` during implementation), new `ManagedCacheAlbumListingInvalidator`/`ManagedCacheUserListingInvalidator` listeners (11 event→tag bindings). New `managed_cache_albums_enabled` config toggle (AND'd with Feature 052's `managed_cache_enabled`), plus the `managed_cache_enabled`/`managed_cache_ttl` migration + `SettingsController` visibility exemption Feature 052 left undone. `make phpstan`: 0 errors. `php-cs-fixer`: 0 violations. See plan.md's Implementation Drift Gate for the handful of implementation-time findings (a default-eager-load pitfall in `BulkEditAlbumsAction`, two more `TypeError`-bug-class endpoints, and a test-isolation config leak in `AlbumRepositoryTest`, all fixed). |
| 052 | Managed Cache Service | 2026-07-28 | All 22 tasks (T-052-01..22) implemented and green. New `App\Services\Cache\ManagedCacheService` (`remember()`/`forgetTag()`/`addTags()`, hand-rolled key-list tag bookkeeping — no native cache-tagging store required, works on the default `file` driver), gated by DB-backed `managed_cache_enabled`/`managed_cache_ttl` (read via `ConfigManager`, not `config()`). Fixed three confirmed invalidation gaps: `Actions\Album\Move::do()` (also dispatches `AlbumSaved` for the album's *previous* parent, not just the new one — needed so both old and new parent's cached listings invalidate), `SharingController` (create/edit/delete/propagate), `UserGroupsManagementController` (addUser/removeUser/updateUserRole) all now dispatch events. New `ManagedCacheAlbumInvalidator` (7 events → album+parent tag eviction) and `ManagedCacheUserInvalidator` (1 event → user tag) listeners. `AlbumRepository::getChildrenPaginated()` and `PhotoRepository::getPhotosForAlbumPaginated()` adopt the service. Q-052-01..05 resolved 2026-07-21; two more questions found while grounding the plan (2026-07-28) — Q-052-06 (`AlbumDeleted` payload gap, resolved Option A — evict parent tag only) and Q-052-07 (Settings category visibility, resolved **Option B**, the non-default choice — share `'Mod Cache'` with a two-key filter exemption, per explicit user override of the recommended new-category option). `php artisan test`: full suite run to completion once (2896 passed / 3 failed — 2 were this feature's own test bug, since fixed and re-verified; 1 pre-existing/unrelated, confirmed via `git stash`); a second full run confirmed all Feature-052 test classes green before being cut short by an unrelated pre-existing `set_time_limit(600)` process-wide timing issue in unrelated Artisan commands (documented in tasks.md, out of scope to fix here). `make phpstan`: 0 errors. `npm run check`/`npm run format`: clean (no frontend files touched). |
| 051 | v8 Admin Setup Page | 2026-07-26 | Implementation complete (T-051-01..12,15). New `CreateInitialAdmin` action shared by legacy Blade `SetUpAdminController` and new `AdminSetupController` (`POST /Admin::Setup`); `GET /setup-admin` route + `ToAdminSetter` branch on `nuxt_ui` flag (ADR-0007); v8 `AdminSetupPage.vue` + `admin-setup-service.ts`; `admin-setup` route added to shared `paths.ts` with v7 `Placeholder.vue` fallback; 22-locale translations. `php artisan test`: all green (incl. new `CreateInitialAdminTest`, `AdminSetupTest` x2). `make phpstan`: 0 errors on touched files. `npm run check`: clean. Q-051-05 (no JS test runner in this repo) resolved by the user (Option A — accept the gap, no dependency added). Manual browser verification not performed this session, to avoid mutating the dev environment; HTTP-level behaviour covered by feature tests instead. |
| 050 | Album Tags | 2026-07-12 | 19/20 tasks implemented and green (T-050-17 manual browser verification not run — sandbox's frontend toolchain broken independent of this feature, confirmed pre-existing via `git stash`). New `albums_tags` pivot + `Album::tags()`/`Tag::albums()` relations, unified with the existing photo/tag-album `Tag` vocabulary. Surfaced on `/tag/{id}` (new Albums section), `Search` (`tag:` modifier + plain-text match, Album-only — never `TagAlbum`, NFR-050-01), and `/tags` (split `num_photos`/`num_albums` counts, album-only tags now visible). `TagCleanupTrait`/`MergeTag`/`DeleteTag` extended so album-only tags are never silently purged by the existing cleanup pass. `PATCH /Album`'s `tags` field is optional (`sometimes`, not `present`) so the legacy v7 frontend — which also calls this endpoint and predates this feature — never has its tags wiped; v8-only for new UI (NFR-050-02), with two 1-line v7 fixes required to keep it compiling against the renamed `TagResource` fields. All 3 open questions resolved (Q-050-01/02/03, all Option A). `php artisan test`: 2798 passed, 2 pre-existing/unrelated failures (timezone-dependent `PhotoEditTest`, confirmed via `git stash`). `make phpstan`: 0 errors. Translations added for all 22 locales. |
| 049 | Migration to Nuxt UI | 2026-07-03 | Spec/plan/tasks drafted; analysis gate passed. 48 tasks (T-049-00..45 incl. sub-tasks) across 15 phases. Builds Nuxt UI (`@nuxt/ui`, standalone Vue mode) as a **parallel tree** `resources/js/v8/**`, served by a second Vite entry (`app-v8.ts`) selected per-request by a `nuxt_ui` feature flag, at the **same routes** as the existing PrimeVue app (`resources/js/app.ts`, untouched until cutover) — supersedes the original in-place migration mechanism (Q-049-04, ADR-0006 amends ADR-0005). Icon parity via `@iconify-json/prime` (Q-049-02 A); ripple dropped entirely (Q-049-03 A); full scope tracked as one feature (Q-049-01 A). New v8-only seams: `useAppToast()`, `useConfirmDialog()`. Embed bundle out of scope. ADR-0005 + ADR-0006 recorded. |
| 048 | Fix Multi-Group Permissions | 2026-07-01 | Spec, plan, tasks drafted. 11 tasks across 7 increments. Fixes `BaseAlbumImpl::current_user_permissions()` using `Collection::first()` (order-dependent) instead of merging every matching `AccessPermission` row (direct-user + all groups) via boolean OR. Merged result returned as a new non-persistable DTO (`App\DTO\EffectiveAccessPermission`, `final readonly class`) instead of a synthetic `AccessPermission` model instance, so it cannot be mass-assigned/`save()`d by accident. Zero new DB queries (NFR-048-01). Q-048-01 resolved (Option A — merge everything, most-permissive-wins). ADR-0004 planned. |
| 047 | Person Smart Album | 2026-06-28 | Spec, plan, tasks drafted. 37 tasks across 14 increments. Mirrors TagAlbum pattern: PersonAlbum model, HasManyPhotosByPerson relation, AND/OR person matching, feature-gated by v8 + ai_vision_face_enabled. |
| 046 | Tag Album Custom Cover | 2026-06-28 | Spec, plan, tasks drafted. All 3 questions resolved (Q-046-01 B, Q-046-02 B, Q-046-03 N/A). Add `cover_id` to `tag_albums` table (not `base_albums`). 5 increments planned (I1 migration, I2 models, I3 API, I4 frontend, I5 tests), 14 tasks. Includes `PhotosToBeDeletedDTO` cover nullification (FR-046-10). |
| 045 | NSFW Detection & Moderation | 2026-06-21 | All 7 increments implemented (I1–I7). Backend: 7 enums, 3 migrations (12 config keys, 2 photo columns, nsfw_detections table), NsfwDetection model, NsfwDetectionService + NsfwActionService, DispatchNsfwScanJob + ApplyNsfwAlbumSensitivityJob, AutoScanNsfwOnUpload pipe, NsfwDetectionController + NsfwConfigController, callback/bulk-scan/config-proxy routes, CSRF exemption, ModerationController NSFW approval logic, Delete::forceDeletePhoto(). Frontend: NsfwConfig.vue admin page, MaintenanceBulkScanNsfw component, nsfw-detection-service.ts + nsfw-config-service.ts, Moderation NSFW badge, admin dashboard tile, translation keys. |
| 044 | Folder Drag-and-Drop Album Creation | 2026-06-13 | Spec, plan, tasks drafted. 14 tasks across 5 increments (I0 type extension, I1 UploadPanel, I2 folderDrop composable, I3 uploadEvents, I4 view wiring). Frontend-only — no backend changes. |
| 043 | Webshop Print & Pixel Sizes | 2026-05-31 | Spec stub created. Blocked on 5 open questions (Q-043-01 … Q-043-05): pricing model, license-type applicability, pixel fulfillment, catalogue scope, SE gating. |
| 042 | Photo Display Enrichment | 2026-06-12 | Part A (I1–I6) complete: `album_title` + `thumb_url` on `OrderItemResource`, unconditional eager-load in `OrderResource`, thumbnail/album-title UI in `OrderDownload.vue`, 4 backend tests passing, PHPStan 0, php-cs-fixer clean. |
| 041 | Upload Photo Metadata | 2026-05-31 | `title`, `description` at upload time; `expected_id` in response. New `ApplyUserProvidedMetadata` pipe, DTO chain propagation (`ImportParam → InitDTO → StandaloneDTO`), `Photo::preallocateId()`, `UploadPhotoRequest` validation, `UploadMetaResource` fields, `ProcessImageJob` serialisation. 9 feature tests passing. PHPStan 0, php-cs-fixer clean. |
| 040 | Disable Request Caching | 2026-05-18 | Spec, plan, tasks drafted. 9 tasks across 5 increments (I1 migration, I2 feature flag + .env.example, I3 controller filter, I4 feature tests, I5 quality gates). |
| 037 | Admin Dashboard & `/admin/` URL Reorg | 2026-04-22 | Config migration (`use_admin_dashboard` toggle), `AdminStatsService` with 5-min cache, `GET /api/v2/Admin/Stats` endpoint, 9 admin views relocated to `views/admin/`, `AdminDashboard.vue` tile grid + stats panel + Refresh, left-menu collapse toggle, 22-locale i18n, 13 backend tests passing, TypeScript/PHPStan clean. |
| 034 | Bulk Album Edit | 2026-04-12 | Spec, plan, tasks drafted. 25 tasks across 11 increments (I1 backend scaffold, I2-I6 REST endpoints, I7-I10 frontend, I11 quality gates). 4 open questions (Q-034-01 to Q-034-04; 1 high, 2 medium, 1 low). |
| 032 | Security Advisories Check | 2026-04-06 | Spec, plan, tasks drafted. 18 tasks across 6 increments (I1 config/DTO, I2 fetch service, I3 diagnostic pipe, I4 REST endpoint, I5 frontend modal, I6 quality gates). All open questions resolved in spec. |
| 030 | AI Vision Service | 2026-03-15 | Spec, plan, tasks drafted. 43 tasks across 19 increments. |
| 029 | Camera Capture | 2026-03-18 | "Take Photo" in `+` add menu (album and root views). CameraCapture.vue modal: live video → canvas capture → JPEG preview → push to existing UploadPanel queue. Secure-context guard, mobile layout fixes, `Permissions-Policy: camera=(self)` header. No backend changes. |
| 028 | Search UI Refactor | 2026-05-30 | Full refactor: simple input + collapsible advanced panel (17 fields: title, description, location, tags, date range, type, orientation, rating, EXIF fields). Token assembler/parser composable. No-debounce on-demand search. Auto-scroll to first result. vue-tsc clean, 74 PHP tests passed, PHPStan 0 errors. |
| 026 | Album Photo Tag Filter | 2026-03-09 | Spec, plan, tasks complete. 76 tasks across 9 increments (~32h estimated). All open questions resolved. |
| 025 | Dynamic Landing Background Options | 2026-03-03 | Spec, plan, tasks completed |
| 024 | CLI Sync File-List Support | 2026-03-03 | `lychee:sync` now accepts individual file paths alongside directories; `Exec::doFiles()` added; `ImportImageJob` accepts nullable Album; 7 new tests. |
| 023 | Remember Me Login | 2026-03-01 | Spec, plan, tasks drafted. Implementation completed. |
| 022 | Contact Form | 2026-03-01 | Spec, plan, and tasks drafted. Supports-only feature: visitor form (public page), admin message management page, configurable sample Q&A, security question, consent text, privacy URL, custom submit button. 16 increments planned (~16 hours). Implementation completed. |
| 021 | Album Variant ZIP Download | 2026-02-28 | Spec, plan, tasks drafted. Starting implementation. |
| 020 | Raw Upload Support | 2026-02-28 | All 47 tasks done. RAW=0 enum shift, 4 migrations, RawToJpeg converter, DetectAndStoreRaw + CreateRawSizeVariant pipes, download gating, frontend RAW download button (22 langs), 38 tests passing. PHPStan 0 errors, php-cs-fixer clean, knowledge map + image-processing ref updated. |
| 019 | Friendly URLs (Album Slugs) | 2026-02-28 | All 24 tasks done. Migration, model, SlugRule, middleware, update requests, feature tests (26 tests/1048 assertions), frontend UI, translations (22 langs). Quality gates: PHPStan 0 errors, php-cs-fixer clean, npm build/check/format clean. |
| 018 | Photo Albums Sidebar  | 2026-02-26 | Spec, plan, tasks drafted. Pending implementation. |
| 017 | Apply Renamer Rules & Watermark Confirm  | 2026-02-26 | Spec, plan, tasks drafted. Pending implementation. |
| 016 | Bulk License Edit | 2026-02-27 | Backend complete (T-016-01 to T-016-04), next: quality gates & frontend |
| 015 | Upload Watermark Toggle | 2026-02-24 | Per-upload watermark control: UI toggle in upload dialog, backend API parameter (apply_watermark), ApplyWatermark pipe respects flag, admin setting (watermark_optout_disabled) to enforce watermarking, translations in 22 languages, end-to-end flow complete (9 increments: I0-I8b) |
| 013 | Starred to Highlighted Rename | 2026-02-22 | Renamed is_starred → is_highlighted, StarredAlbum → HighlightedAlbum, auto-inserts 5-star rating for highlighted photos, config key rename, translations for 22 languages, 29 tasks complete |
| 012 | Embeddable Photo Album Widget | 2026-02-20 | JavaScript widget for embedding albums/photo streams on external websites, supports all gallery layouts (square/justified/masonry/grid/film), lightbox, CORS API, theme customization, embed code generator UI |
| 011 | My Rated Pictures Smart Albums | 2026-01-28 | Two new smart albums filtering by user ratings: MyRatedPicturesAlbum (all photos rated by current user), MyBestPicturesAlbum (top N rated with tie-inclusive logic), hidden from guests, requires SE license for best pictures, translations in 21 languages |
| 010 | LDAP Authentication Support | 2026-01-26 | Enterprise directory integration with auto-provisioning, role mapping via groups, TLS/SSL support, graceful degradation to local auth, comprehensive logging, full documentation (11 increments: I1-I11 complete) |
| 009 | Rating Ordering and Smart Albums | 2026-01-28 | Photo ordering by rating (average, user-specific), smart albums for rating ranges (Unrated, 1-5 Stars, Best Pictures with configurable count and tie-inclusive logic), translations in 21 languages |
| 008 | Shared Albums Visibility Control | 2026-01-28 | Server-side filtering for shared albums, visibility controls, admin configuration UI for share management |
| 007 | Photos and Albums Pagination | 2026-01-14 | New paginated API endpoints (/Album/{id}/head, /albums, /photos), configurable page sizes, three UI modes (infinite scroll, load more, page navigation), Smart/Tag album support |
| 006 | Photo Star Rating Filter | 2026-01-14 | Frontend filter control (5 clickable stars) for minimum rating threshold, toggle on/off behavior, Pinia state persistence, keyboard accessible, filters photos in album view |
| 005 | Album List View Toggle | 2026-01-04 | Toggle between grid/card and list view for albums, admin-configurable default, session-only user preference, full RTL support, drag-select compatible |
| 004 | Album Size Statistics Pre-computation | 2026-01-02 | Pre-computed album_size_statistics table with 7 size variant columns, RecomputeAlbumSizeJob with deduplication, Spaces.php refactored to use pre-computed values, event-driven updates |
| 003 | Album Computed Fields Pre-computation | 2026-01-02 | Event-driven pre-computation for 6 album fields (num_children, num_photos, min/max_taken_at, dual auto covers), AlbumBuilder virtual column removal, backfill/recovery commands, comprehensive test coverage |
| 002 | Worker Mode Support | 2025-12-28 | Docker worker mode with queue processing, auto-restart, configurable QUEUE_NAMES/WORKER_MAX_TIME, multi-container deployment |
| 001 | Photo Star Rating | 2025-12-27 | User ratings (1-5 stars), statistics aggregation, configurable visibility |

## Backlog

| Feature ID | Name | Priority | Notes |
|------------|------|----------|-------|
| _No backlog items yet_ | | | |

## Feature Directory Structure

Features are organized under `docs/specs/4-architecture/features/<NNN>-<feature-name>/`:

```
features/
├── 001-feature-name/
│   ├── spec.md    # Feature specification
│   ├── plan.md    # Implementation plan
│   └── tasks.md   # Task checklist
└── 002-another-feature/
    ├── spec.md
    ├── plan.md
    └── tasks.md
```

## How to Use This Roadmap

1. **Start a new feature:**
   - Assign next available feature ID (format: `###`)
   - Create feature directory: `features/<NNN>-<feature-name>/`
   - Author `spec.md` using [templates/feature-spec-template.md](../../templates/feature-spec-template.md)
   - Add row to Active Features table

2. **Track progress:**
   - Update Status column (Planning → In Progress → Testing → Complete)
   - Update timestamps regularly
   - Create `plan.md` and `tasks.md` once spec is approved

3. **Complete a feature:**
   - Move row from Active to Completed
   - Archive or remove feature directory
   - Update related knowledge map entries

4. **Add to backlog:**
   - Add row to Backlog table
   - No feature directory needed until promoted to Active

## Status Values

- **Planning** - Specification in progress
- **In Progress** - Active implementation
- **Testing** - Code complete, under verification
- **Blocked** - Waiting on dependencies or clarification
- **Complete** - Delivered and verified

## Priority Levels

- **P0** - Critical (security, data loss, blocking production)
- **P1** - High (major features, significant user impact)
- **P2** - Medium (enhancements, minor features)
- **P3** - Low (nice-to-have, future considerations)

---

*Last updated: 2026-09-02 (Feature 062 implemented — all 32 tasks complete — moved to Completed Features; Feature 061 implemented — all 38 tasks complete — moved to Completed Features; Feature 060 moved to Completed Features; Feature 059 implemented, moved to Completed Features)*
